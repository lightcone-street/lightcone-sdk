// Generated by ReScript, PLEASE EDIT WITH CARE


function _quickStart(url) {
  let socket = new WebSocket(url);
  socket.onopen = () => {
    socket.send(`{"type":"subscribe"}`);
  };
  socket.onmessage = event => {};
  socket.onerror = _event => {};
  socket.onclose = event => {};
  return socket;
}

function _isOpen(socket) {
  return socket.readyState === 1;
}

function _send(socket, text) {
  socket.send(text);
}

function _close(socket) {
  socket.close();
}

function _closeWith(socket) {
  socket.close(1000, "bye");
}

function _readMessage(event) {
  return event.data;
}

function _readClose(event) {
  return [
    event.code,
    event.reason
  ];
}

function _messageType(event) {
  return event.type;
}

export {
  _quickStart,
  _isOpen,
  _send,
  _close,
  _closeWith,
  _readMessage,
  _readClose,
  _messageType,
}
/* No side effect */
