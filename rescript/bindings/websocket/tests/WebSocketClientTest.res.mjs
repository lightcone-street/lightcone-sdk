// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Buntest from "bun:test";

Buntest.describe("WebSocketClient (smoke)", () => {
  Buntest.test("make constructs a socket and readyState is an int in 0..3", () => {
    let socket = new WebSocket("ws://localhost:1");
    socket.onerror = _event => {};
    let state = socket.readyState;
    Buntest.expect(state >= 0 && state <= 3).toBe(true);
    socket.close();
  });
  Buntest.test("every handler setter is callable and event accessors type-check", () => {
    let socket = new WebSocket("ws://localhost:1");
    socket.onerror = _event => {};
    socket.onopen = () => {};
    socket.onmessage = event => {};
    socket.onclose = event => {};
    Buntest.expect(socket.readyState >= 0).toBe(true);
    socket.close();
  });
});

/*  Not a pure module */
