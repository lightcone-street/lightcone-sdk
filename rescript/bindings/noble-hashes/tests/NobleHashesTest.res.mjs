// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Buntest from "bun:test";
import * as Sha3Js from "@noble/hashes/sha3.js";

let utf8 = ((text) => new TextEncoder().encode(text));

let toHex = ((bytes) => Array.from(bytes).map((b) => b.toString(16).padStart(2, "0")).join(""));

let byteLength = ((bytes) => bytes.length);

let keccakAbc = "4e03657aea45a94fc7d47ba826c8d667c0d1e6e33a64a036ec44f58fa12d6c45";

Buntest.describe("NobleHashes", () => {
  Buntest.test("keccak256(\"abc\") matches the canonical vector (lowercase hex)", () => {
    Buntest.expect(toHex(Sha3Js.keccak_256(utf8("abc")))).toBe(keccakAbc);
  });
  Buntest.test("keccak256 returns a 32-byte digest", () => {
    Buntest.expect(byteLength(Sha3Js.keccak_256(utf8("hello world")))).toBe(32);
  });
  Buntest.test("keccak256 is deterministic", () => {
    let once = toHex(Sha3Js.keccak_256(utf8("lightcone")));
    let twice = toHex(Sha3Js.keccak_256(utf8("lightcone")));
    Buntest.expect(once).toBe(twice);
  });
});

export {
  utf8,
  toHex,
  byteLength,
  keccakAbc,
}
/*  Not a pure module */
