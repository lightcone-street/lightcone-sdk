// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Decimal from "../Decimal.res.mjs";
import * as Buntest from "bun:test";
import DecimalJs from "decimal.js";

Buntest.describe("Decimal — construction", () => {
  Buntest.test("fromString builds an exact decimal", () => {
    Buntest.expect(new DecimalJs("1.5").toString()).toBe("1.5");
  });
  Buntest.test("fromInt builds from an int", () => {
    Buntest.expect(new DecimalJs(42).toString()).toBe("42");
  });
  Buntest.test("fromFloat builds from a JS number", () => {
    Buntest.expect(new DecimalJs(2.5).toString()).toBe("2.5");
  });
});

Buntest.describe("Decimal — arithmetic", () => {
  Buntest.test("times: 1.5 * 2 -> toFixed(1) == \"3.0\"", () => {
    Buntest.expect(new DecimalJs("1.5").times(new DecimalJs(2)).toFixed(1)).toBe("3.0");
  });
  Buntest.test("plus is exact: 0.1 + 0.2 == 0.3 (no binary float drift)", () => {
    Buntest.expect(new DecimalJs("0.1").plus(new DecimalJs("0.2")).toFixed(1)).toBe("0.3");
  });
  Buntest.test("minus: 10 - 3 == 7", () => {
    Buntest.expect(new DecimalJs(10).minus(new DecimalJs(3)).toString()).toBe("7");
  });
  Buntest.test("div: 10 / 4 == 2.5", () => {
    Buntest.expect(new DecimalJs(10).div(new DecimalJs(4)).toString()).toBe("2.5");
  });
  Buntest.test("pow: 2 ^ 10 == 1024 (exponent is a Decimal)", () => {
    Buntest.expect(new DecimalJs(2).pow(new DecimalJs(10)).toString()).toBe("1024");
  });
  Buntest.test("powInt: 10 ^ 3 == 1000 (int exponent convenience)", () => {
    Buntest.expect(new DecimalJs(10).pow(3).toString()).toBe("1000");
  });
});

Buntest.describe("Decimal — rounding and sign", () => {
  Buntest.test("abs: |-5.5| == 5.5", () => {
    Buntest.expect(new DecimalJs("-5.5").abs().toString()).toBe("5.5");
  });
  Buntest.test("floor: 2.7 -> 2", () => {
    Buntest.expect(new DecimalJs("2.7").floor().toString()).toBe("2");
  });
  Buntest.test("ceil: 2.3 -> 3", () => {
    Buntest.expect(new DecimalJs("2.3").ceil().toString()).toBe("3");
  });
  Buntest.test("round: 2.6 -> 3 (ROUND_HALF_UP default)", () => {
    Buntest.expect(new DecimalJs("2.6").round().toString()).toBe("3");
  });
  Buntest.test("round: 2.4 -> 2", () => {
    Buntest.expect(new DecimalJs("2.4").round().toString()).toBe("2");
  });
  Buntest.test("toSignificantDigits: 123.456 at 2 sig digits -> 120", () => {
    Buntest.expect(new DecimalJs("123.456").toSignificantDigits(2).toString()).toBe("120");
  });
  Buntest.test("toDecimalPlaces with roundDown truncates: 1.2399 at 2 dp -> 1.23", () => {
    Buntest.expect(new DecimalJs("1.2399").toDecimalPlaces(2, Decimal.roundDown).toString()).toBe("1.23");
  });
});

Buntest.describe("Decimal — comparison and predicates", () => {
  Buntest.test("cmp: 1 vs 2 == -1", () => {
    Buntest.expect(new DecimalJs(1).cmp(new DecimalJs(2))).toBe(-1);
  });
  Buntest.test("cmp: 2 vs 2 == 0", () => {
    Buntest.expect(new DecimalJs(2).cmp(new DecimalJs(2))).toBe(0);
  });
  Buntest.test("cmp: 3 vs 2 == 1", () => {
    Buntest.expect(new DecimalJs(3).cmp(new DecimalJs(2))).toBe(1);
  });
  Buntest.test("eq: 1.0 == 1", () => {
    Buntest.expect(new DecimalJs("1.0").eq(new DecimalJs(1))).toBe(true);
  });
  Buntest.test("gt: 2 > 1", () => {
    Buntest.expect(new DecimalJs(2).gt(new DecimalJs(1))).toBe(true);
  });
  Buntest.test("gte: 2 >= 2", () => {
    Buntest.expect(new DecimalJs(2).gte(new DecimalJs(2))).toBe(true);
  });
  Buntest.test("lt: 1 < 2", () => {
    Buntest.expect(new DecimalJs(1).lt(new DecimalJs(2))).toBe(true);
  });
  Buntest.test("lte: 2 <= 2", () => {
    Buntest.expect(new DecimalJs(2).lte(new DecimalJs(2))).toBe(true);
  });
  Buntest.test("isZero: 0 is zero, 1 is not", () => {
    Buntest.expect(new DecimalJs(0).isZero()).toBe(true);
    Buntest.expect(new DecimalJs(1).isZero()).toBe(false);
  });
  Buntest.test("isNeg: -1 is negative, 1 is not", () => {
    Buntest.expect(new DecimalJs("-1").isNeg()).toBe(true);
    Buntest.expect(new DecimalJs(1).isNeg()).toBe(false);
  });
});

Buntest.describe("Decimal — terminal accessors", () => {
  Buntest.test("toFixed pads to the requested places: 1.5 at 3 -> \"1.500\"", () => {
    Buntest.expect(new DecimalJs("1.5").toFixed(3)).toBe("1.500");
  });
  Buntest.test("toString gives the canonical form", () => {
    Buntest.expect(new DecimalJs(42).toString()).toBe("42");
  });
  Buntest.test("toNumber returns a JS float", () => {
    Buntest.expect(new DecimalJs("3.14").toNumber()).toBe(3.14);
  });
});

/*  Not a pure module */
