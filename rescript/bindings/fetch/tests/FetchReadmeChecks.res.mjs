// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Stdlib_JsExn from "@rescript/runtime/lib/es6/Stdlib_JsExn.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Primitive_option from "@rescript/runtime/lib/es6/Primitive_option.js";
import * as Primitive_exceptions from "@rescript/runtime/lib/es6/Primitive_exceptions.js";

async function _quickStart(url) {
  let response = await fetch(url, {
    method: "GET"
  });
  if (response.ok) {
    return await response.json();
  }
}

async function _withRecovery(url) {
  let response;
  try {
    response = await fetch(url, {
      method: "GET"
    });
  } catch (raw_error) {
    let error = Primitive_exceptions.internalToException(raw_error);
    if (error.RE_EXN_ID === "JsExn") {
      return {
        TAG: "Error",
        _0: Stdlib_Option.getOr(Stdlib_JsExn.message(error._1), "network error")
      };
    }
    throw error;
  }
  return {
    TAG: "Ok",
    _0: response
  };
}

function _readResponse(response) {
  
}

async function _readBodies(response) {
  await response.text();
  await response.json();
}

function _decodeOk(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return;
  }
  let match = json["ok"];
  if (typeof match === "boolean") {
    return match;
  }
}

function _contentType(response) {
  return Primitive_option.fromNull(response.headers.get("content-type"));
}

let _full_method = "POST";

let _full_headers = Object.fromEntries([[
    "content-type",
    "application/json"
  ]]);

let _full_body = `{"ping":true}`;

let _full_credentials = "include";

let _full_signal = Primitive_option.some(AbortSignal.timeout(180000));

let _full = {
  method: _full_method,
  headers: _full_headers,
  body: _full_body,
  credentials: _full_credentials,
  signal: _full_signal
};

function _abort() {
  let controller = new AbortController();
  controller.abort();
}

let _timeout = AbortSignal.timeout(5000);

let _readSetCookies = ((h) => h.getSetCookie?.() ?? []);

let _justMethod = {
  method: "GET"
};

let _bare = {};

export {
  _quickStart,
  _withRecovery,
  _readResponse,
  _readBodies,
  _decodeOk,
  _contentType,
  _justMethod,
  _bare,
  _full,
  _abort,
  _timeout,
  _readSetCookies,
}
/* _full Not a pure module */
