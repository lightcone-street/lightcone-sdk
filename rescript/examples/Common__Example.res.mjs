// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Env from "../src/Env.res.mjs";
import * as Nodefs from "node:fs";
import * as Nodeos from "node:os";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as TypeScriptApi from "../src/TypeScriptApi.res.mjs";

function client() {
  let env = Stdlib_Option.getOr(Stdlib_Option.flatMap(Env.getEnv("LIGHTCONE_ENV"), Env.fromString), "prod");
  return TypeScriptApi.make(env, undefined, undefined, undefined, undefined, undefined, undefined, undefined);
}

let secretKeyFromJson = ((raw) => Uint8Array.from(JSON.parse(raw)));

function walletSecretKey() {
  let path = Stdlib_Option.getOr(Env.getEnv("LIGHTCONE_WALLET_PATH"), "~/.config/solana/id.json");
  let resolved = path.startsWith("~") ? path.replace("~", Nodeos.homedir()) : path;
  return secretKeyFromJson(Nodefs.readFileSync(resolved, "utf-8"));
}

export {
  client,
  secretKeyFromJson,
  walletSecretKey,
}
/* node:fs Not a pure module */
