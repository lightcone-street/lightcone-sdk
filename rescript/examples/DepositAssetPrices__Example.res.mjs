// Generated by ReScript, PLEASE EDIT WITH CARE

import * as SdkError from "../src/SdkError.res.mjs";
import * as PriceHistory from "../src/domain/PriceHistory.res.mjs";
import * as Common__Example from "./Common__Example.res.mjs";

async function main() {
  let client = Common__Example.client();
  let error = await PriceHistory.getDepositAssetPricesSnapshot(client);
  if (error.TAG === "Ok") {
    let entries = Object.entries(error._0.prices);
    console.log(`deposit-asset-prices-snapshot: ` + entries.length.toString() + ` entries`);
    entries.slice(0, 10).forEach(param => {
      console.log(`  ` + param[0] + ` -> ` + param[1]);
    });
    return;
  }
  console.error(SdkError.toMessage(error._0));
}

main();

export {
  main,
}
/*  Not a pure module */
