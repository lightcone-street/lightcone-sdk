// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Metrics from "../src/domain/Metrics.res.mjs";
import * as SdkError from "../src/SdkError.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Common__Example from "./Common__Example.res.mjs";

async function main() {
  let client = Common__Example.client();
  let depositAsset = process.argv[2];
  let response = await Metrics.orderbookTickers(client, depositAsset);
  if (response.TAG === "Ok") {
    let response$1 = response._0;
    console.log(`orderbooks with tickers: ` + response$1.tickers.length.toString());
    response$1.tickers.slice(0, 10).forEach(entry => {
      let mid = Stdlib_Option.getOr(entry.midpoint, "—");
      let outcome = Stdlib_Option.mapOr(entry.outcomeIndex, "—", value => value.toString());
      console.log(`  ` + entry.orderbookId + ` (market ` + entry.marketPubkey + `, outcome ` + outcome + `) mid=` + mid);
    });
    return;
  }
  console.error(SdkError.toMessage(response._0));
}

main();

export {
  main,
}
/*  Not a pure module */
