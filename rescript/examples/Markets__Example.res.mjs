// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Market from "../src/domain/Market.res.mjs";
import * as SdkError from "../src/SdkError.res.mjs";
import * as Common__Example from "./Common__Example.res.mjs";

async function main() {
  let client = Common__Example.client();
  let featured = await Market.featured(client);
  if (featured.TAG === "Ok") {
    console.log(`Featured markets: ` + featured._0.length.toString());
  } else {
    console.error(SdkError.toMessage(featured._0));
  }
  let error = await Market.get(client, undefined, 5);
  if (error.TAG === "Ok") {
    let markets = error._0.markets;
    console.log(`First ` + markets.length.toString() + ` markets:`);
    markets.forEach(market => {
      console.log(`  ` + market.slug + ` — ` + market.name);
    });
    return;
  }
  console.error(SdkError.toMessage(error._0));
}

main();

export {
  main,
}
/*  Not a pure module */
