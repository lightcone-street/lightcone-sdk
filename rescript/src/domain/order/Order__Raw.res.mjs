// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Spice from "@mununki/ppx-spice/src/rescript/Spice.res.mjs";
import * as Shared from "../../Shared.res.mjs";
import DecimalJs from "decimal.js";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Stdlib_Result from "@rescript/runtime/lib/es6/Stdlib_Result.js";
import * as Notification__Raw from "../notification/Notification__Raw.res.mjs";
import * as Primitive_exceptions from "@rescript/runtime/lib/es6/Primitive_exceptions.js";

function optString(dict, key) {
  let match = dict[key];
  if (typeof match === "string") {
    return match;
  }
}

function optFloat(dict, key) {
  let match = dict[key];
  if (typeof match === "number") {
    return match;
  }
}

function decimalOr0(dict, key) {
  return Stdlib_Option.getOr(optString(dict, key), "0");
}

function orderStatusFieldDecode(dict) {
  let json = dict["status"];
  if (json !== undefined) {
    return Shared.OrderStatus.t_decode(json);
  } else {
    return {
      TAG: "Ok",
      _0: "OPEN"
    };
  }
}

function tifNumericDecode(dict, key, json) {
  let match = dict[key];
  if (match !== undefined) {
    if (typeof match === "number") {
      if (match < 2.0) {
        if (match !== 0.0) {
          if (match !== 1.0) {
            return Spice.error(undefined, "unknown numeric tif value", json);
          } else {
            return {
              TAG: "Ok",
              _0: "IOC"
            };
          }
        } else {
          return {
            TAG: "Ok",
            _0: "GTC"
          };
        }
      } else if (match !== 2.0) {
        if (match !== 3.0) {
          return Spice.error(undefined, "unknown numeric tif value", json);
        } else {
          return {
            TAG: "Ok",
            _0: "ALO"
          };
        }
      } else {
        return {
          TAG: "Ok",
          _0: "FOK"
        };
      }
    } else {
      return Spice.error(undefined, "unknown numeric tif value", json);
    }
  } else {
    return {
      TAG: "Ok",
      _0: "GTC"
    };
  }
}

function tifNumericOptDecode(dict, key, json) {
  let match = dict[key];
  if (match !== undefined) {
    return Stdlib_Result.map(tifNumericDecode(dict, key, json), value => value);
  } else {
    return {
      TAG: "Ok",
      _0: undefined
    };
  }
}

function tifToNumber(tif) {
  switch (tif) {
    case "GTC" :
      return 0.0;
    case "IOC" :
      return 1.0;
    case "FOK" :
      return 2.0;
    case "ALO" :
      return 3.0;
  }
}

function amountWithAlias(dict, key, alias) {
  let value = optString(dict, key);
  if (value !== undefined) {
    return value;
  } else {
    return optString(dict, alias);
  }
}

function decimalIsPositive(value) {
  let decimal;
  try {
    decimal = new DecimalJs(value);
  } catch (raw_exn) {
    let exn = Primitive_exceptions.internalToException(raw_exn);
    if (exn.RE_EXN_ID === "JsExn") {
      return false;
    }
    throw exn;
  }
  return decimal.gt(new DecimalJs(0));
}

function decimalOrZero(value) {
  try {
    return new DecimalJs(value);
  } catch (raw_exn) {
    let exn = Primitive_exceptions.internalToException(raw_exn);
    if (exn.RE_EXN_ID === "JsExn") {
      return new DecimalJs(0);
    }
    throw exn;
  }
}

function sizeOf(filled, remaining) {
  return decimalOrZero(filled).plus(decimalOrZero(remaining)).toString();
}

function t_encode(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "counterparty",
      Shared.pubkeyStr_encodeJson(v.counterparty)
    ],
    [
      "counterparty_order_hash",
      Spice.stringToJson(v.counterpartyOrderHash)
    ],
    [
      "fill_amount",
      Spice.stringToJson(v.fillAmount)
    ],
    [
      "price",
      Spice.stringToJson(v.price)
    ],
    [
      "is_maker",
      Spice.boolToJson(v.isMaker)
    ]
  ]));
}

function t_encodeJson(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "counterparty",
      Shared.pubkeyStr_encodeJson(v.counterparty)
    ],
    [
      "counterparty_order_hash",
      Spice.stringToJson(v.counterpartyOrderHash)
    ],
    [
      "fill_amount",
      Spice.stringToJson(v.fillAmount)
    ],
    [
      "price",
      Spice.stringToJson(v.price)
    ],
    [
      "is_maker",
      Spice.boolToJson(v.isMaker)
    ]
  ]));
}

function t_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let counterparty = Stdlib_Option.getOr(Stdlib_Option.map(v["counterparty"], Shared.pubkeyStr_decode), Spice.error(undefined, "counterparty" + " missing", v));
  if (counterparty.TAG === "Ok") {
    let counterpartyOrderHash = Stdlib_Option.getOr(Stdlib_Option.map(v["counterparty_order_hash"], Spice.stringFromJson), Spice.error(undefined, "counterparty_order_hash" + " missing", v));
    if (counterpartyOrderHash.TAG === "Ok") {
      let fillAmount = Stdlib_Option.getOr(Stdlib_Option.map(v["fill_amount"], Spice.stringFromJson), Spice.error(undefined, "fill_amount" + " missing", v));
      if (fillAmount.TAG === "Ok") {
        let price = Stdlib_Option.getOr(Stdlib_Option.map(v["price"], Spice.stringFromJson), Spice.error(undefined, "price" + " missing", v));
        if (price.TAG === "Ok") {
          let isMaker = Stdlib_Option.getOr(Stdlib_Option.map(v["is_maker"], Spice.boolFromJson), Spice.error(undefined, "is_maker" + " missing", v));
          if (isMaker.TAG === "Ok") {
            return {
              TAG: "Ok",
              _0: {
                counterparty: counterparty._0,
                counterpartyOrderHash: counterpartyOrderHash._0,
                fillAmount: fillAmount._0,
                price: price._0,
                isMaker: isMaker._0
              }
            };
          }
          let e = isMaker._0;
          return Spice.error("." + ("is_maker" + e.path), e.message, e.value);
        }
        let e$1 = price._0;
        return Spice.error("." + ("price" + e$1.path), e$1.message, e$1.value);
      }
      let e$2 = fillAmount._0;
      return Spice.error("." + ("fill_amount" + e$2.path), e$2.message, e$2.value);
    }
    let e$3 = counterpartyOrderHash._0;
    return Spice.error("." + ("counterparty_order_hash" + e$3.path), e$3.message, e$3.value);
  }
  let e$4 = counterparty._0;
  return Spice.error("." + ("counterparty" + e$4.path), e$4.message, e$4.value);
}

let FillInfo = {
  t_encode: t_encode,
  t_encodeJson: t_encodeJson,
  t_decode: t_decode
};

function t_encode$1(v) {
  switch (v) {
    case "accepted" :
      return "accepted";
    case "partial_fill" :
      return "partial_fill";
    case "filled" :
      return "filled";
  }
}

function t_encodeJson$1(v) {
  switch (v) {
    case "accepted" :
      return "accepted";
    case "partial_fill" :
      return "partial_fill";
    case "filled" :
      return "filled";
  }
}

function t_decode$1(v) {
  switch (typeof v) {
    case "string" :
      if ("accepted" === v) {
        return {
          TAG: "Ok",
          _0: "accepted"
        };
      } else if ("partial_fill" === v) {
        return {
          TAG: "Ok",
          _0: "partial_fill"
        };
      } else if ("filled" === v) {
        return {
          TAG: "Ok",
          _0: "filled"
        };
      } else {
        return Spice.error(undefined, "Not matched", v);
      }
    case "number" :
      return Spice.error(undefined, "Not matched", v);
    default:
      return Spice.error(undefined, "Not a JSONString", v);
  }
}

let SubmitStatus = {
  t_encode: t_encode$1,
  t_encodeJson: t_encodeJson$1,
  t_decode: t_decode$1
};

function t_encode$2(v) {
  let extra = v.fills;
  return Object.fromEntries(Spice.filterOptional([
    [
      "order_hash",
      Spice.stringToJson(v.orderHash)
    ],
    [
      "status",
      t_encodeJson$1(v.status)
    ],
    [
      "remaining",
      Spice.stringToJson(v.remaining)
    ],
    [
      "filled",
      Spice.stringToJson(v.filled)
    ],
    [
      "fills",
      Spice.arrayToJson(t_encodeJson, extra)
    ]
  ]));
}

function t_encodeJson$2(v) {
  let extra = v.fills;
  return Object.fromEntries(Spice.filterOptional([
    [
      "order_hash",
      Spice.stringToJson(v.orderHash)
    ],
    [
      "status",
      t_encodeJson$1(v.status)
    ],
    [
      "remaining",
      Spice.stringToJson(v.remaining)
    ],
    [
      "filled",
      Spice.stringToJson(v.filled)
    ],
    [
      "fills",
      Spice.arrayToJson(t_encodeJson, extra)
    ]
  ]));
}

function t_decode$2(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let orderHash = Stdlib_Option.getOr(Stdlib_Option.map(v["order_hash"], Spice.stringFromJson), Spice.error(undefined, "order_hash" + " missing", v));
  if (orderHash.TAG === "Ok") {
    let status = Stdlib_Option.getOr(Stdlib_Option.map(v["status"], t_decode$1), Spice.error(undefined, "status" + " missing", v));
    if (status.TAG === "Ok") {
      let remaining = Stdlib_Option.getOr(Stdlib_Option.map(v["remaining"], Spice.stringFromJson), Spice.error(undefined, "remaining" + " missing", v));
      if (remaining.TAG === "Ok") {
        let filled = Stdlib_Option.getOr(Stdlib_Option.map(v["filled"], Spice.stringFromJson), Spice.error(undefined, "filled" + " missing", v));
        if (filled.TAG === "Ok") {
          let fills = Stdlib_Option.getOr(Stdlib_Option.map(v["fills"], extra => Spice.arrayFromJson(t_decode, extra)), Spice.error(undefined, "fills" + " missing", v));
          if (fills.TAG === "Ok") {
            return {
              TAG: "Ok",
              _0: {
                orderHash: orderHash._0,
                status: status._0,
                remaining: remaining._0,
                filled: filled._0,
                fills: fills._0
              }
            };
          }
          let e = fills._0;
          return Spice.error("." + ("fills" + e.path), e.message, e.value);
        }
        let e$1 = filled._0;
        return Spice.error("." + ("filled" + e$1.path), e$1.message, e$1.value);
      }
      let e$2 = remaining._0;
      return Spice.error("." + ("remaining" + e$2.path), e$2.message, e$2.value);
    }
    let e$3 = status._0;
    return Spice.error("." + ("status" + e$3.path), e$3.message, e$3.value);
  }
  let e$4 = orderHash._0;
  return Spice.error("." + ("order_hash" + e$4.path), e$4.message, e$4.value);
}

let SubmitResponse = {
  t_encode: t_encode$2,
  t_encodeJson: t_encodeJson$2,
  t_decode: t_decode$2
};

function toJson(request) {
  let fields = [
    [
      "maker",
      request.maker
    ],
    [
      "nonce",
      Number(request.nonce)
    ],
    [
      "salt",
      Number(request.salt)
    ],
    [
      "market_pubkey",
      request.marketPubkey
    ],
    [
      "base_token",
      request.baseToken
    ],
    [
      "quote_token",
      request.quoteToken
    ],
    [
      "side",
      request.side
    ],
    [
      "amount_in",
      Number(request.amountIn)
    ],
    [
      "amount_out",
      Number(request.amountOut)
    ],
    [
      "expiration",
      Number(request.expiration)
    ],
    [
      "signature",
      request.signatureHex
    ],
    [
      "orderbook_id",
      request.orderbookId
    ]
  ];
  Stdlib_Option.forEach(request.timeInForce, tif => {
    fields.push([
      "tif",
      Shared.TimeInForce.t_encode(tif)
    ]);
  });
  Stdlib_Option.forEach(request.depositSource, source => {
    fields.push([
      "deposit_source",
      Shared.DepositSource.t_encode(source)
    ]);
  });
  Stdlib_Option.forEach(request.triggerPrice, price => {
    fields.push([
      "trigger_price",
      price
    ]);
  });
  Stdlib_Option.forEach(request.triggerType, triggerType => {
    fields.push([
      "trigger_type",
      Shared.TriggerType.t_encode(triggerType)
    ]);
  });
  return Object.fromEntries(fields);
}

let SubmitRequest = {
  toJson: toJson
};

function t_encode$3(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "trigger_order_id",
      Spice.stringToJson(v.triggerOrderId)
    ],
    [
      "order_hash",
      Spice.stringToJson(v.orderHash)
    ]
  ]));
}

function t_encodeJson$3(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "trigger_order_id",
      Spice.stringToJson(v.triggerOrderId)
    ],
    [
      "order_hash",
      Spice.stringToJson(v.orderHash)
    ]
  ]));
}

function t_decode$3(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let triggerOrderId = Stdlib_Option.getOr(Stdlib_Option.map(v["trigger_order_id"], Spice.stringFromJson), Spice.error(undefined, "trigger_order_id" + " missing", v));
  if (triggerOrderId.TAG === "Ok") {
    let orderHash = Stdlib_Option.getOr(Stdlib_Option.map(v["order_hash"], Spice.stringFromJson), Spice.error(undefined, "order_hash" + " missing", v));
    if (orderHash.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: {
          triggerOrderId: triggerOrderId._0,
          orderHash: orderHash._0
        }
      };
    }
    let e = orderHash._0;
    return Spice.error("." + ("order_hash" + e.path), e.message, e.value);
  }
  let e$1 = triggerOrderId._0;
  return Spice.error("." + ("trigger_order_id" + e$1.path), e$1.message, e$1.value);
}

let TriggerResponse = {
  t_encode: t_encode$3,
  t_encodeJson: t_encodeJson$3,
  t_decode: t_decode$3
};

function t_encode$4(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "order_hash",
      Spice.stringToJson(v.orderHash)
    ],
    [
      "remaining",
      Spice.stringToJson(v.remaining)
    ]
  ]));
}

function t_encodeJson$4(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "order_hash",
      Spice.stringToJson(v.orderHash)
    ],
    [
      "remaining",
      Spice.stringToJson(v.remaining)
    ]
  ]));
}

function t_decode$4(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let orderHash = Stdlib_Option.getOr(Stdlib_Option.map(v["order_hash"], Spice.stringFromJson), Spice.error(undefined, "order_hash" + " missing", v));
  if (orderHash.TAG === "Ok") {
    let remaining = Stdlib_Option.getOr(Stdlib_Option.map(v["remaining"], Spice.stringFromJson), Spice.error(undefined, "remaining" + " missing", v));
    if (remaining.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: {
          orderHash: orderHash._0,
          remaining: remaining._0
        }
      };
    }
    let e = remaining._0;
    return Spice.error("." + ("remaining" + e.path), e.message, e.value);
  }
  let e$1 = orderHash._0;
  return Spice.error("." + ("order_hash" + e$1.path), e$1.message, e$1.value);
}

let CancelSuccess = {
  t_encode: t_encode$4,
  t_encodeJson: t_encodeJson$4,
  t_decode: t_decode$4
};

function t_encode$5(v) {
  let extra = v.cancelledOrderHashes;
  return Object.fromEntries(Spice.filterOptional([
    [
      "cancelled_order_hashes",
      Spice.arrayToJson(Spice.stringToJson, extra)
    ],
    [
      "count",
      Spice.floatToJson(v.count)
    ],
    [
      "user_pubkey",
      Shared.pubkeyStr_encodeJson(v.userPubkey)
    ],
    [
      "orderbook_id",
      Shared.orderBookId_encodeJson(v.orderbookId)
    ],
    [
      "message",
      Spice.stringToJson(v.message)
    ]
  ]));
}

function t_encodeJson$5(v) {
  let extra = v.cancelledOrderHashes;
  return Object.fromEntries(Spice.filterOptional([
    [
      "cancelled_order_hashes",
      Spice.arrayToJson(Spice.stringToJson, extra)
    ],
    [
      "count",
      Spice.floatToJson(v.count)
    ],
    [
      "user_pubkey",
      Shared.pubkeyStr_encodeJson(v.userPubkey)
    ],
    [
      "orderbook_id",
      Shared.orderBookId_encodeJson(v.orderbookId)
    ],
    [
      "message",
      Spice.stringToJson(v.message)
    ]
  ]));
}

function t_decode$5(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let cancelledOrderHashes = Stdlib_Option.getOr(Stdlib_Option.map(v["cancelled_order_hashes"], extra => Spice.arrayFromJson(Spice.stringFromJson, extra)), Spice.error(undefined, "cancelled_order_hashes" + " missing", v));
  if (cancelledOrderHashes.TAG === "Ok") {
    let count = Stdlib_Option.getOr(Stdlib_Option.map(v["count"], Spice.floatFromJson), Spice.error(undefined, "count" + " missing", v));
    if (count.TAG === "Ok") {
      let userPubkey = Stdlib_Option.getOr(Stdlib_Option.map(v["user_pubkey"], Shared.pubkeyStr_decode), Spice.error(undefined, "user_pubkey" + " missing", v));
      if (userPubkey.TAG === "Ok") {
        let orderbookId = Stdlib_Option.getOr(Stdlib_Option.map(v["orderbook_id"], Shared.orderBookId_decode), Spice.error(undefined, "orderbook_id" + " missing", v));
        if (orderbookId.TAG === "Ok") {
          let message = Stdlib_Option.getOr(Stdlib_Option.map(v["message"], Spice.stringFromJson), Spice.error(undefined, "message" + " missing", v));
          if (message.TAG === "Ok") {
            return {
              TAG: "Ok",
              _0: {
                cancelledOrderHashes: cancelledOrderHashes._0,
                count: count._0,
                userPubkey: userPubkey._0,
                orderbookId: orderbookId._0,
                message: message._0
              }
            };
          }
          let e = message._0;
          return Spice.error("." + ("message" + e.path), e.message, e.value);
        }
        let e$1 = orderbookId._0;
        return Spice.error("." + ("orderbook_id" + e$1.path), e$1.message, e$1.value);
      }
      let e$2 = userPubkey._0;
      return Spice.error("." + ("user_pubkey" + e$2.path), e$2.message, e$2.value);
    }
    let e$3 = count._0;
    return Spice.error("." + ("count" + e$3.path), e$3.message, e$3.value);
  }
  let e$4 = cancelledOrderHashes._0;
  return Spice.error("." + ("cancelled_order_hashes" + e$4.path), e$4.message, e$4.value);
}

let CancelAllSuccess = {
  t_encode: t_encode$5,
  t_encodeJson: t_encodeJson$5,
  t_decode: t_decode$5
};

function t_encode$6(v) {
  return Object.fromEntries(Spice.filterOptional([[
      "trigger_order_id",
      Spice.stringToJson(v.triggerOrderId)
    ]]));
}

function t_encodeJson$6(v) {
  return Object.fromEntries(Spice.filterOptional([[
      "trigger_order_id",
      Spice.stringToJson(v.triggerOrderId)
    ]]));
}

function t_decode$6(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let triggerOrderId = Stdlib_Option.getOr(Stdlib_Option.map(v["trigger_order_id"], Spice.stringFromJson), Spice.error(undefined, "trigger_order_id" + " missing", v));
  if (triggerOrderId.TAG === "Ok") {
    return {
      TAG: "Ok",
      _0: {
        triggerOrderId: triggerOrderId._0
      }
    };
  }
  let e = triggerOrderId._0;
  return Spice.error("." + ("trigger_order_id" + e.path), e.message, e.value);
}

let CancelTriggerSuccess = {
  t_encode: t_encode$6,
  t_encodeJson: t_encodeJson$6,
  t_decode: t_decode$6
};

let CancelBody = {};

let CancelAllBody = {};

let CancelTriggerBody = {};

function t_decode$7(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "snapshot order is not an object", json);
  }
  let match = optString(json, "order_hash");
  let match$1 = optString(json, "market_pubkey");
  let match$2 = optString(json, "orderbook_id");
  let match$3 = optString(json, "base_mint");
  let match$4 = optString(json, "quote_mint");
  let match$5 = json["side"];
  if (match === undefined) {
    return Spice.error(undefined, "snapshot order missing required fields", json);
  }
  if (match$1 === undefined) {
    return Spice.error(undefined, "snapshot order missing required fields", json);
  }
  if (match$2 === undefined) {
    return Spice.error(undefined, "snapshot order missing required fields", json);
  }
  if (match$3 === undefined) {
    return Spice.error(undefined, "snapshot order missing required fields", json);
  }
  if (match$4 === undefined) {
    return Spice.error(undefined, "snapshot order missing required fields", json);
  }
  if (match$5 === undefined) {
    return Spice.error(undefined, "snapshot order missing required fields", json);
  }
  let match$6 = Shared.Side.t_decode(match$5);
  let match$7 = orderStatusFieldDecode(json);
  let match$8 = amountWithAlias(json, "amount_in", "maker_amount");
  let match$9 = amountWithAlias(json, "amount_out", "taker_amount");
  let match$10 = optFloat(json, "created_at");
  let match$11 = optFloat(json, "outcome_index");
  if (match$6.TAG === "Ok") {
    if (match$7.TAG === "Ok") {
      if (match$8 !== undefined && match$9 !== undefined && match$10 !== undefined && match$11 !== undefined) {
        return {
          TAG: "Ok",
          _0: {
            orderHash: match,
            marketPubkey: match$1,
            orderbookId: match$2,
            side: match$6._0,
            amountIn: match$8,
            amountOut: match$9,
            remaining: decimalOr0(json, "remaining"),
            filled: decimalOr0(json, "filled"),
            price: decimalOr0(json, "price"),
            createdAt: match$10,
            expiration: Stdlib_Option.getOr(optFloat(json, "expiration"), 0.0),
            baseMint: match$3,
            quoteMint: match$4,
            outcomeIndex: match$11,
            status: match$7._0
          }
        };
      } else {
        return Spice.error(undefined, "snapshot order missing amount_in/amount_out/created_at/outcome_index", json);
      }
    } else {
      return {
        TAG: "Error",
        _0: match$7._0
      };
    }
  } else {
    return {
      TAG: "Error",
      _0: match$6._0
    };
  }
}

function commonFields(common) {
  return [
    [
      "order_hash",
      common.orderHash
    ],
    [
      "market_pubkey",
      common.marketPubkey
    ],
    [
      "orderbook_id",
      common.orderbookId
    ],
    [
      "side",
      Shared.Side.t_encode(common.side)
    ],
    [
      "amount_in",
      common.amountIn
    ],
    [
      "amount_out",
      common.amountOut
    ],
    [
      "remaining",
      common.remaining
    ],
    [
      "filled",
      common.filled
    ],
    [
      "price",
      common.price
    ],
    [
      "created_at",
      common.createdAt
    ],
    [
      "expiration",
      common.expiration
    ],
    [
      "base_mint",
      common.baseMint
    ],
    [
      "quote_mint",
      common.quoteMint
    ],
    [
      "outcome_index",
      common.outcomeIndex
    ],
    [
      "status",
      Shared.OrderStatus.t_encode(common.status)
    ]
  ];
}

function toLimit(snapshot) {
  return {
    marketPubkey: snapshot.common.marketPubkey,
    orderbookId: snapshot.common.orderbookId,
    txSignature: snapshot.txSignature,
    baseMint: snapshot.common.baseMint,
    quoteMint: snapshot.common.quoteMint,
    orderHash: snapshot.common.orderHash,
    side: snapshot.common.side,
    size: sizeOf(snapshot.common.filled, snapshot.common.remaining),
    price: snapshot.common.price,
    filledSize: snapshot.common.filled,
    remainingSize: snapshot.common.remaining,
    createdAt: snapshot.common.createdAt,
    status: snapshot.common.status,
    outcomeIndex: snapshot.common.outcomeIndex
  };
}

let SnapshotLimit = {
  toLimit: toLimit
};

function toTrigger(snapshot) {
  return {
    triggerOrderId: snapshot.triggerOrderId,
    orderHash: snapshot.common.orderHash,
    marketPubkey: snapshot.common.marketPubkey,
    orderbookId: snapshot.common.orderbookId,
    triggerPrice: snapshot.triggerPrice,
    triggerType: snapshot.triggerType,
    side: snapshot.common.side,
    amountIn: snapshot.common.amountIn,
    amountOut: snapshot.common.amountOut,
    timeInForce: Stdlib_Option.getOr(snapshot.timeInForce, "GTC"),
    createdAt: snapshot.common.createdAt
  };
}

let SnapshotTrigger = {
  toTrigger: toTrigger
};

function common(order) {
  return order._0.common;
}

function t_decode$8(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "snapshot order is not an object", json);
  }
  let match = json["order_type"];
  if (match === undefined) {
    return Spice.error(undefined, "unknown snapshot order_type", json);
  }
  if (typeof match !== "string") {
    return Spice.error(undefined, "unknown snapshot order_type", json);
  }
  switch (match) {
    case "limit" :
      return Stdlib_Result.map(t_decode$7(json), common => ({
        TAG: "Limit",
        _0: {
          common: common,
          txSignature: optString(json, "tx_signature")
        }
      }));
    case "trigger" :
      let match$1 = t_decode$7(json);
      let match$2 = optString(json, "trigger_order_id");
      let match$3 = optString(json, "trigger_price");
      let match$4 = Stdlib_Option.map(json["trigger_type"], Shared.TriggerType.t_decode);
      let match$5 = tifNumericOptDecode(json, "time_in_force", json);
      let exit = 0;
      if (match$1.TAG !== "Ok") {
        return {
          TAG: "Error",
          _0: match$1._0
        };
      }
      if (match$2 !== undefined && match$3 !== undefined) {
        if (match$4 !== undefined) {
          if (match$4.TAG === "Ok") {
            if (match$5.TAG === "Ok") {
              return {
                TAG: "Ok",
                _0: {
                  TAG: "Trigger",
                  _0: {
                    common: match$1._0,
                    triggerOrderId: match$2,
                    triggerPrice: match$3,
                    triggerType: match$4._0,
                    timeInForce: match$5._0
                  }
                }
              };
            }
          } else {
            exit = 2;
          }
        }
      } else {
        exit = 2;
      }
      if (exit === 2 && match$4 !== undefined && match$4.TAG !== "Ok") {
        return {
          TAG: "Error",
          _0: match$4._0
        };
      }
      if (match$5.TAG === "Ok") {
        return Spice.error(undefined, "trigger snapshot order missing trigger fields", json);
      } else {
        return {
          TAG: "Error",
          _0: match$5._0
        };
      }
      break;
    default:
      return Spice.error(undefined, "unknown snapshot order_type", json);
  }
}

function t_encode$7(order) {
  if (order.TAG === "Limit") {
    let order$1 = order._0;
    let fields = [[
        "order_type",
        "limit"
      ]];
    fields.push(...commonFields(order$1.common));
    Stdlib_Option.forEach(order$1.txSignature, value => {
      fields.push([
        "tx_signature",
        value
      ]);
    });
    return Object.fromEntries(fields);
  }
  let order$2 = order._0;
  let fields$1 = [[
      "order_type",
      "trigger"
    ]];
  fields$1.push(...commonFields(order$2.common));
  fields$1.push([
    "trigger_order_id",
    order$2.triggerOrderId
  ]);
  fields$1.push([
    "trigger_price",
    order$2.triggerPrice
  ]);
  fields$1.push([
    "trigger_type",
    Shared.TriggerType.t_encode(order$2.triggerType)
  ]);
  Stdlib_Option.forEach(order$2.timeInForce, tif => {
    fields$1.push([
      "time_in_force",
      tifToNumber(tif)
    ]);
  });
  return Object.fromEntries(fields$1);
}

let SnapshotOrder = {
  common: common,
  t_decode: t_decode$8,
  t_encode: t_encode$7,
  t_encodeJson: t_encode$7
};

function t_encode$8(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "outcome_index",
      Spice.floatToJson(v.outcomeIndex)
    ],
    [
      "conditional_token",
      Shared.pubkeyStr_encodeJson(v.conditionalToken)
    ],
    [
      "balance",
      Spice.stringToJson(v.balance)
    ],
    [
      "balance_idle",
      Spice.stringToJson(v.balanceIdle)
    ],
    [
      "balance_on_book",
      Spice.stringToJson(v.balanceOnBook)
    ]
  ]));
}

function t_encodeJson$7(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "outcome_index",
      Spice.floatToJson(v.outcomeIndex)
    ],
    [
      "conditional_token",
      Shared.pubkeyStr_encodeJson(v.conditionalToken)
    ],
    [
      "balance",
      Spice.stringToJson(v.balance)
    ],
    [
      "balance_idle",
      Spice.stringToJson(v.balanceIdle)
    ],
    [
      "balance_on_book",
      Spice.stringToJson(v.balanceOnBook)
    ]
  ]));
}

function t_decode$9(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let outcomeIndex = Stdlib_Option.getOr(Stdlib_Option.map(v["outcome_index"], Spice.floatFromJson), Spice.error(undefined, "outcome_index" + " missing", v));
  if (outcomeIndex.TAG === "Ok") {
    let conditionalToken = Stdlib_Option.getOr(Stdlib_Option.map(v["conditional_token"], Shared.pubkeyStr_decode), Spice.error(undefined, "conditional_token" + " missing", v));
    if (conditionalToken.TAG === "Ok") {
      let balance = Stdlib_Option.getOr(Stdlib_Option.map(v["balance"], Spice.stringFromJson), Spice.error(undefined, "balance" + " missing", v));
      if (balance.TAG === "Ok") {
        let balanceIdle = Stdlib_Option.getOr(Stdlib_Option.map(v["balance_idle"], Spice.stringFromJson), Spice.error(undefined, "balance_idle" + " missing", v));
        if (balanceIdle.TAG === "Ok") {
          let balanceOnBook = Stdlib_Option.getOr(Stdlib_Option.map(v["balance_on_book"], Spice.stringFromJson), Spice.error(undefined, "balance_on_book" + " missing", v));
          if (balanceOnBook.TAG === "Ok") {
            return {
              TAG: "Ok",
              _0: {
                outcomeIndex: outcomeIndex._0,
                conditionalToken: conditionalToken._0,
                balance: balance._0,
                balanceIdle: balanceIdle._0,
                balanceOnBook: balanceOnBook._0
              }
            };
          }
          let e = balanceOnBook._0;
          return Spice.error("." + ("balance_on_book" + e.path), e.message, e.value);
        }
        let e$1 = balanceIdle._0;
        return Spice.error("." + ("balance_idle" + e$1.path), e$1.message, e$1.value);
      }
      let e$2 = balance._0;
      return Spice.error("." + ("balance" + e$2.path), e$2.message, e$2.value);
    }
    let e$3 = conditionalToken._0;
    return Spice.error("." + ("conditional_token" + e$3.path), e$3.message, e$3.value);
  }
  let e$4 = outcomeIndex._0;
  return Spice.error("." + ("outcome_index" + e$4.path), e$4.message, e$4.value);
}

function isZero(balance) {
  return !(decimalIsPositive(balance.balanceIdle) || decimalIsPositive(balance.balanceOnBook));
}

let UserOutcomeBalance = {
  t_encode: t_encode$8,
  t_encodeJson: t_encodeJson$7,
  t_decode: t_decode$9,
  isZero: isZero
};

function t_encode$9(v) {
  let extra = v.outcomes;
  return Object.fromEntries(Spice.filterOptional([
    [
      "deposit_asset",
      Shared.pubkeyStr_encodeJson(v.depositAsset)
    ],
    [
      "outcomes",
      Spice.arrayToJson(t_encodeJson$7, extra)
    ]
  ]));
}

function t_encodeJson$8(v) {
  let extra = v.outcomes;
  return Object.fromEntries(Spice.filterOptional([
    [
      "deposit_asset",
      Shared.pubkeyStr_encodeJson(v.depositAsset)
    ],
    [
      "outcomes",
      Spice.arrayToJson(t_encodeJson$7, extra)
    ]
  ]));
}

function t_decode$10(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let depositAsset = Stdlib_Option.getOr(Stdlib_Option.map(v["deposit_asset"], Shared.pubkeyStr_decode), Spice.error(undefined, "deposit_asset" + " missing", v));
  if (depositAsset.TAG === "Ok") {
    let outcomes = Stdlib_Option.getOr(Stdlib_Option.map(v["outcomes"], extra => Spice.arrayFromJson(t_decode$9, extra)), Spice.error(undefined, "outcomes" + " missing", v));
    if (outcomes.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: {
          depositAsset: depositAsset._0,
          outcomes: outcomes._0
        }
      };
    }
    let e = outcomes._0;
    return Spice.error("." + ("outcomes" + e.path), e.message, e.value);
  }
  let e$1 = depositAsset._0;
  return Spice.error("." + ("deposit_asset" + e$1.path), e$1.message, e$1.value);
}

let UserDepositAssetBalance = {
  t_encode: t_encode$9,
  t_encodeJson: t_encodeJson$8,
  t_decode: t_decode$10
};

function t_encode$10(v) {
  let extra = v.depositAssets;
  return Object.fromEntries(Spice.filterOptional([
    [
      "market_pubkey",
      Shared.pubkeyStr_encodeJson(v.marketPubkey)
    ],
    [
      "deposit_assets",
      Spice.arrayToJson(t_encodeJson$8, extra)
    ]
  ]));
}

function t_encodeJson$9(v) {
  let extra = v.depositAssets;
  return Object.fromEntries(Spice.filterOptional([
    [
      "market_pubkey",
      Shared.pubkeyStr_encodeJson(v.marketPubkey)
    ],
    [
      "deposit_assets",
      Spice.arrayToJson(t_encodeJson$8, extra)
    ]
  ]));
}

function t_decode$11(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let marketPubkey = Stdlib_Option.getOr(Stdlib_Option.map(v["market_pubkey"], Shared.pubkeyStr_decode), Spice.error(undefined, "market_pubkey" + " missing", v));
  if (marketPubkey.TAG === "Ok") {
    let depositAssets = Stdlib_Option.getOr(Stdlib_Option.map(v["deposit_assets"], extra => Spice.arrayFromJson(t_decode$10, extra)), Spice.error(undefined, "deposit_assets" + " missing", v));
    if (depositAssets.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: {
          marketPubkey: marketPubkey._0,
          depositAssets: depositAssets._0
        }
      };
    }
    let e = depositAssets._0;
    return Spice.error("." + ("deposit_assets" + e.path), e.message, e.value);
  }
  let e$1 = marketPubkey._0;
  return Spice.error("." + ("market_pubkey" + e$1.path), e$1.message, e$1.value);
}

let UserMarketBalance = {
  t_encode: t_encode$10,
  t_encodeJson: t_encodeJson$9,
  t_decode: t_decode$11
};

function t_encode$11(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "mint",
      Shared.pubkeyStr_encodeJson(v.mint)
    ],
    [
      "balance",
      Spice.stringToJson(v.balance)
    ]
  ]));
}

function t_encodeJson$10(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "mint",
      Shared.pubkeyStr_encodeJson(v.mint)
    ],
    [
      "balance",
      Spice.stringToJson(v.balance)
    ]
  ]));
}

function t_decode$12(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let mint = Stdlib_Option.getOr(Stdlib_Option.map(v["mint"], Shared.pubkeyStr_decode), Spice.error(undefined, "mint" + " missing", v));
  if (mint.TAG === "Ok") {
    let balance = Stdlib_Option.getOr(Stdlib_Option.map(v["balance"], Spice.stringFromJson), Spice.error(undefined, "balance" + " missing", v));
    if (balance.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: {
          mint: mint._0,
          balance: balance._0
        }
      };
    }
    let e = balance._0;
    return Spice.error("." + ("balance" + e.path), e.message, e.value);
  }
  let e$1 = mint._0;
  return Spice.error("." + ("mint" + e$1.path), e$1.message, e$1.value);
}

let GlobalDepositBalance = {
  t_encode: t_encode$11,
  t_encodeJson: t_encodeJson$10,
  t_decode: t_decode$12
};

function t_encode$12(v) {
  let extra = v.orders;
  let extra$1 = v.marketBalances;
  let extra$2 = v.nextCursor;
  return Object.fromEntries(Spice.filterOptional([
    [
      "user_pubkey",
      Shared.pubkeyStr_encodeJson(v.userPubkey)
    ],
    [
      "orders",
      Spice.arrayToJson(t_encode$7, extra)
    ],
    [
      "market_balances",
      Spice.arrayToJson(t_encodeJson$9, extra$1)
    ],
    [
      "next_cursor",
      Spice.optionToJson(Spice.stringToJson, extra$2)
    ],
    [
      "has_more",
      Spice.boolToJson(v.hasMore)
    ]
  ]));
}

function t_encodeJson$11(v) {
  let extra = v.orders;
  let extra$1 = v.marketBalances;
  let extra$2 = v.nextCursor;
  return Object.fromEntries(Spice.filterOptional([
    [
      "user_pubkey",
      Shared.pubkeyStr_encodeJson(v.userPubkey)
    ],
    [
      "orders",
      Spice.arrayToJson(t_encode$7, extra)
    ],
    [
      "market_balances",
      Spice.arrayToJson(t_encodeJson$9, extra$1)
    ],
    [
      "next_cursor",
      Spice.optionToJson(Spice.stringToJson, extra$2)
    ],
    [
      "has_more",
      Spice.boolToJson(v.hasMore)
    ]
  ]));
}

function t_decode$13(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let userPubkey = Stdlib_Option.getOr(Stdlib_Option.map(v["user_pubkey"], Shared.pubkeyStr_decode), Spice.error(undefined, "user_pubkey" + " missing", v));
  if (userPubkey.TAG === "Ok") {
    let orders = Stdlib_Option.getOr(Stdlib_Option.map(v["orders"], extra => Spice.arrayFromJson(t_decode$8, extra)), Spice.error(undefined, "orders" + " missing", v));
    if (orders.TAG === "Ok") {
      let marketBalances = Stdlib_Option.getOr(Stdlib_Option.map(v["market_balances"], extra => Spice.arrayFromJson(t_decode$11, extra)), Spice.error(undefined, "market_balances" + " missing", v));
      if (marketBalances.TAG === "Ok") {
        let nextCursor = Stdlib_Option.getOr(Stdlib_Option.map(v["next_cursor"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
          TAG: "Ok",
          _0: undefined
        });
        if (nextCursor.TAG === "Ok") {
          let hasMore = Stdlib_Option.getOr(Stdlib_Option.map(v["has_more"], Spice.boolFromJson), {
            TAG: "Ok",
            _0: false
          });
          if (hasMore.TAG === "Ok") {
            return {
              TAG: "Ok",
              _0: {
                userPubkey: userPubkey._0,
                orders: orders._0,
                marketBalances: marketBalances._0,
                nextCursor: nextCursor._0,
                hasMore: hasMore._0
              }
            };
          }
          let e = hasMore._0;
          return Spice.error("." + ("has_more" + e.path), e.message, e.value);
        }
        let e$1 = nextCursor._0;
        return Spice.error("." + ("next_cursor" + e$1.path), e$1.message, e$1.value);
      }
      let e$2 = marketBalances._0;
      return Spice.error("." + ("market_balances" + e$2.path), e$2.message, e$2.value);
    }
    let e$3 = orders._0;
    return Spice.error("." + ("orders" + e$3.path), e$3.message, e$3.value);
  }
  let e$4 = userPubkey._0;
  return Spice.error("." + ("user_pubkey" + e$4.path), e$4.message, e$4.value);
}

let UserOrdersResponse = {
  t_encode: t_encode$12,
  t_encodeJson: t_encodeJson$11,
  t_decode: t_decode$13
};

function t_encode$13(v) {
  if (v === "maker") {
    return "maker";
  } else {
    return "taker";
  }
}

function t_encodeJson$12(v) {
  if (v === "maker") {
    return "maker";
  } else {
    return "taker";
  }
}

function t_decode$14(v) {
  switch (typeof v) {
    case "string" :
      if ("maker" === v) {
        return {
          TAG: "Ok",
          _0: "maker"
        };
      } else if ("taker" === v) {
        return {
          TAG: "Ok",
          _0: "taker"
        };
      } else {
        return Spice.error(undefined, "Not matched", v);
      }
    case "number" :
      return Spice.error(undefined, "Not matched", v);
    default:
      return Spice.error(undefined, "Not a JSONString", v);
  }
}

let Role = {
  t_encode: t_encode$13,
  t_encodeJson: t_encodeJson$12,
  t_decode: t_decode$14
};

function t_encode$14(v) {
  switch (v) {
    case "filled" :
      return "filled";
    case "cancelled" :
      return "cancelled";
    case "partially_filled" :
      return "partially_filled";
  }
}

function t_encodeJson$13(v) {
  switch (v) {
    case "filled" :
      return "filled";
    case "cancelled" :
      return "cancelled";
    case "partially_filled" :
      return "partially_filled";
  }
}

function t_decode$15(v) {
  switch (typeof v) {
    case "string" :
      if ("filled" === v) {
        return {
          TAG: "Ok",
          _0: "filled"
        };
      } else if ("cancelled" === v) {
        return {
          TAG: "Ok",
          _0: "cancelled"
        };
      } else if ("partially_filled" === v) {
        return {
          TAG: "Ok",
          _0: "partially_filled"
        };
      } else {
        return Spice.error(undefined, "Not matched", v);
      }
    case "number" :
      return Spice.error(undefined, "Not matched", v);
    default:
      return Spice.error(undefined, "Not a JSONString", v);
  }
}

let FillStatus = {
  t_encode: t_encode$14,
  t_encodeJson: t_encodeJson$13,
  t_decode: t_decode$15
};

function t_encode$15(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "fill_amount",
      Spice.stringToJson(v.fillAmount)
    ],
    [
      "tx_signature",
      Spice.stringToJson(v.txSignature)
    ],
    [
      "filled_at",
      Spice.floatToJson(v.filledAt)
    ]
  ]));
}

function t_encodeJson$14(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "fill_amount",
      Spice.stringToJson(v.fillAmount)
    ],
    [
      "tx_signature",
      Spice.stringToJson(v.txSignature)
    ],
    [
      "filled_at",
      Spice.floatToJson(v.filledAt)
    ]
  ]));
}

function t_decode$16(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let fillAmount = Stdlib_Option.getOr(Stdlib_Option.map(v["fill_amount"], Spice.stringFromJson), Spice.error(undefined, "fill_amount" + " missing", v));
  if (fillAmount.TAG === "Ok") {
    let txSignature = Stdlib_Option.getOr(Stdlib_Option.map(v["tx_signature"], Spice.stringFromJson), Spice.error(undefined, "tx_signature" + " missing", v));
    if (txSignature.TAG === "Ok") {
      let filledAt = Stdlib_Option.getOr(Stdlib_Option.map(v["filled_at"], Spice.floatFromJson), Spice.error(undefined, "filled_at" + " missing", v));
      if (filledAt.TAG === "Ok") {
        return {
          TAG: "Ok",
          _0: {
            fillAmount: fillAmount._0,
            txSignature: txSignature._0,
            filledAt: filledAt._0
          }
        };
      }
      let e = filledAt._0;
      return Spice.error("." + ("filled_at" + e.path), e.message, e.value);
    }
    let e$1 = txSignature._0;
    return Spice.error("." + ("tx_signature" + e$1.path), e$1.message, e$1.value);
  }
  let e$2 = fillAmount._0;
  return Spice.error("." + ("fill_amount" + e$2.path), e$2.message, e$2.value);
}

let FillEvent = {
  t_encode: t_encode$15,
  t_encodeJson: t_encodeJson$14,
  t_decode: t_decode$16
};

function t_encode$16(v) {
  let extra = v.fills;
  return Object.fromEntries(Spice.filterOptional([
    [
      "order_hash",
      Spice.stringToJson(v.orderHash)
    ],
    [
      "market_pubkey",
      Shared.pubkeyStr_encodeJson(v.marketPubkey)
    ],
    [
      "orderbook_id",
      Shared.orderBookId_encodeJson(v.orderbookId)
    ],
    [
      "side",
      Shared.Side.t_encodeJson(v.side)
    ],
    [
      "role",
      t_encodeJson$12(v.role)
    ],
    [
      "price",
      Spice.stringToJson(v.price)
    ],
    [
      "size",
      Spice.stringToJson(v.size)
    ],
    [
      "filled_size",
      Spice.stringToJson(v.filledSize)
    ],
    [
      "remaining_size",
      Spice.stringToJson(v.remainingSize)
    ],
    [
      "base_mint",
      Shared.pubkeyStr_encodeJson(v.baseMint)
    ],
    [
      "quote_mint",
      Shared.pubkeyStr_encodeJson(v.quoteMint)
    ],
    [
      "outcome_index",
      Spice.floatToJson(v.outcomeIndex)
    ],
    [
      "status",
      t_encodeJson$13(v.status)
    ],
    [
      "created_at",
      Spice.floatToJson(v.createdAt)
    ],
    [
      "fills",
      Spice.arrayToJson(t_encodeJson$14, extra)
    ]
  ]));
}

function t_encodeJson$15(v) {
  let extra = v.fills;
  return Object.fromEntries(Spice.filterOptional([
    [
      "order_hash",
      Spice.stringToJson(v.orderHash)
    ],
    [
      "market_pubkey",
      Shared.pubkeyStr_encodeJson(v.marketPubkey)
    ],
    [
      "orderbook_id",
      Shared.orderBookId_encodeJson(v.orderbookId)
    ],
    [
      "side",
      Shared.Side.t_encodeJson(v.side)
    ],
    [
      "role",
      t_encodeJson$12(v.role)
    ],
    [
      "price",
      Spice.stringToJson(v.price)
    ],
    [
      "size",
      Spice.stringToJson(v.size)
    ],
    [
      "filled_size",
      Spice.stringToJson(v.filledSize)
    ],
    [
      "remaining_size",
      Spice.stringToJson(v.remainingSize)
    ],
    [
      "base_mint",
      Shared.pubkeyStr_encodeJson(v.baseMint)
    ],
    [
      "quote_mint",
      Shared.pubkeyStr_encodeJson(v.quoteMint)
    ],
    [
      "outcome_index",
      Spice.floatToJson(v.outcomeIndex)
    ],
    [
      "status",
      t_encodeJson$13(v.status)
    ],
    [
      "created_at",
      Spice.floatToJson(v.createdAt)
    ],
    [
      "fills",
      Spice.arrayToJson(t_encodeJson$14, extra)
    ]
  ]));
}

function t_decode$17(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let orderHash = Stdlib_Option.getOr(Stdlib_Option.map(v["order_hash"], Spice.stringFromJson), Spice.error(undefined, "order_hash" + " missing", v));
  if (orderHash.TAG === "Ok") {
    let marketPubkey = Stdlib_Option.getOr(Stdlib_Option.map(v["market_pubkey"], Shared.pubkeyStr_decode), Spice.error(undefined, "market_pubkey" + " missing", v));
    if (marketPubkey.TAG === "Ok") {
      let orderbookId = Stdlib_Option.getOr(Stdlib_Option.map(v["orderbook_id"], Shared.orderBookId_decode), Spice.error(undefined, "orderbook_id" + " missing", v));
      if (orderbookId.TAG === "Ok") {
        let side = Stdlib_Option.getOr(Stdlib_Option.map(v["side"], Shared.Side.t_decode), Spice.error(undefined, "side" + " missing", v));
        if (side.TAG === "Ok") {
          let role = Stdlib_Option.getOr(Stdlib_Option.map(v["role"], t_decode$14), Spice.error(undefined, "role" + " missing", v));
          if (role.TAG === "Ok") {
            let price = Stdlib_Option.getOr(Stdlib_Option.map(v["price"], Spice.stringFromJson), Spice.error(undefined, "price" + " missing", v));
            if (price.TAG === "Ok") {
              let size = Stdlib_Option.getOr(Stdlib_Option.map(v["size"], Spice.stringFromJson), Spice.error(undefined, "size" + " missing", v));
              if (size.TAG === "Ok") {
                let filledSize = Stdlib_Option.getOr(Stdlib_Option.map(v["filled_size"], Spice.stringFromJson), Spice.error(undefined, "filled_size" + " missing", v));
                if (filledSize.TAG === "Ok") {
                  let remainingSize = Stdlib_Option.getOr(Stdlib_Option.map(v["remaining_size"], Spice.stringFromJson), Spice.error(undefined, "remaining_size" + " missing", v));
                  if (remainingSize.TAG === "Ok") {
                    let baseMint = Stdlib_Option.getOr(Stdlib_Option.map(v["base_mint"], Shared.pubkeyStr_decode), Spice.error(undefined, "base_mint" + " missing", v));
                    if (baseMint.TAG === "Ok") {
                      let quoteMint = Stdlib_Option.getOr(Stdlib_Option.map(v["quote_mint"], Shared.pubkeyStr_decode), Spice.error(undefined, "quote_mint" + " missing", v));
                      if (quoteMint.TAG === "Ok") {
                        let outcomeIndex = Stdlib_Option.getOr(Stdlib_Option.map(v["outcome_index"], Spice.floatFromJson), Spice.error(undefined, "outcome_index" + " missing", v));
                        if (outcomeIndex.TAG === "Ok") {
                          let status = Stdlib_Option.getOr(Stdlib_Option.map(v["status"], t_decode$15), Spice.error(undefined, "status" + " missing", v));
                          if (status.TAG === "Ok") {
                            let createdAt = Stdlib_Option.getOr(Stdlib_Option.map(v["created_at"], Spice.floatFromJson), Spice.error(undefined, "created_at" + " missing", v));
                            if (createdAt.TAG === "Ok") {
                              let fills = Stdlib_Option.getOr(Stdlib_Option.map(v["fills"], extra => Spice.arrayFromJson(t_decode$16, extra)), Spice.error(undefined, "fills" + " missing", v));
                              if (fills.TAG === "Ok") {
                                return {
                                  TAG: "Ok",
                                  _0: {
                                    orderHash: orderHash._0,
                                    marketPubkey: marketPubkey._0,
                                    orderbookId: orderbookId._0,
                                    side: side._0,
                                    role: role._0,
                                    price: price._0,
                                    size: size._0,
                                    filledSize: filledSize._0,
                                    remainingSize: remainingSize._0,
                                    baseMint: baseMint._0,
                                    quoteMint: quoteMint._0,
                                    outcomeIndex: outcomeIndex._0,
                                    status: status._0,
                                    createdAt: createdAt._0,
                                    fills: fills._0
                                  }
                                };
                              }
                              let e = fills._0;
                              return Spice.error("." + ("fills" + e.path), e.message, e.value);
                            }
                            let e$1 = createdAt._0;
                            return Spice.error("." + ("created_at" + e$1.path), e$1.message, e$1.value);
                          }
                          let e$2 = status._0;
                          return Spice.error("." + ("status" + e$2.path), e$2.message, e$2.value);
                        }
                        let e$3 = outcomeIndex._0;
                        return Spice.error("." + ("outcome_index" + e$3.path), e$3.message, e$3.value);
                      }
                      let e$4 = quoteMint._0;
                      return Spice.error("." + ("quote_mint" + e$4.path), e$4.message, e$4.value);
                    }
                    let e$5 = baseMint._0;
                    return Spice.error("." + ("base_mint" + e$5.path), e$5.message, e$5.value);
                  }
                  let e$6 = remainingSize._0;
                  return Spice.error("." + ("remaining_size" + e$6.path), e$6.message, e$6.value);
                }
                let e$7 = filledSize._0;
                return Spice.error("." + ("filled_size" + e$7.path), e$7.message, e$7.value);
              }
              let e$8 = size._0;
              return Spice.error("." + ("size" + e$8.path), e$8.message, e$8.value);
            }
            let e$9 = price._0;
            return Spice.error("." + ("price" + e$9.path), e$9.message, e$9.value);
          }
          let e$10 = role._0;
          return Spice.error("." + ("role" + e$10.path), e$10.message, e$10.value);
        }
        let e$11 = side._0;
        return Spice.error("." + ("side" + e$11.path), e$11.message, e$11.value);
      }
      let e$12 = orderbookId._0;
      return Spice.error("." + ("orderbook_id" + e$12.path), e$12.message, e$12.value);
    }
    let e$13 = marketPubkey._0;
    return Spice.error("." + ("market_pubkey" + e$13.path), e$13.message, e$13.value);
  }
  let e$14 = orderHash._0;
  return Spice.error("." + ("order_hash" + e$14.path), e$14.message, e$14.value);
}

let UserFill = {
  t_encode: t_encode$16,
  t_encodeJson: t_encodeJson$15,
  t_decode: t_decode$17
};

function t_encode$17(v) {
  let extra = v.orders;
  let extra$1 = v.nextCursor;
  return Object.fromEntries(Spice.filterOptional([
    [
      "orders",
      Spice.arrayToJson(t_encodeJson$15, extra)
    ],
    [
      "next_cursor",
      Spice.optionToJson(Spice.stringToJson, extra$1)
    ],
    [
      "has_more",
      Spice.boolToJson(v.hasMore)
    ]
  ]));
}

function t_encodeJson$16(v) {
  let extra = v.orders;
  let extra$1 = v.nextCursor;
  return Object.fromEntries(Spice.filterOptional([
    [
      "orders",
      Spice.arrayToJson(t_encodeJson$15, extra)
    ],
    [
      "next_cursor",
      Spice.optionToJson(Spice.stringToJson, extra$1)
    ],
    [
      "has_more",
      Spice.boolToJson(v.hasMore)
    ]
  ]));
}

function t_decode$18(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let orders = Stdlib_Option.getOr(Stdlib_Option.map(v["orders"], extra => Spice.arrayFromJson(t_decode$17, extra)), Spice.error(undefined, "orders" + " missing", v));
  if (orders.TAG === "Ok") {
    let nextCursor = Stdlib_Option.getOr(Stdlib_Option.map(v["next_cursor"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
      TAG: "Ok",
      _0: undefined
    });
    if (nextCursor.TAG === "Ok") {
      let hasMore = Stdlib_Option.getOr(Stdlib_Option.map(v["has_more"], Spice.boolFromJson), Spice.error(undefined, "has_more" + " missing", v));
      if (hasMore.TAG === "Ok") {
        return {
          TAG: "Ok",
          _0: {
            orders: orders._0,
            nextCursor: nextCursor._0,
            hasMore: hasMore._0
          }
        };
      }
      let e = hasMore._0;
      return Spice.error("." + ("has_more" + e.path), e.message, e.value);
    }
    let e$1 = nextCursor._0;
    return Spice.error("." + ("next_cursor" + e$1.path), e$1.message, e$1.value);
  }
  let e$2 = orders._0;
  return Spice.error("." + ("orders" + e$2.path), e$2.message, e$2.value);
}

let UserFillsResponse = {
  t_encode: t_encode$17,
  t_encodeJson: t_encodeJson$16,
  t_decode: t_decode$18
};

function t_encode$18(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "outcome_index",
      Spice.floatToJson(v.outcomeIndex)
    ],
    [
      "conditional_token",
      Shared.pubkeyStr_encodeJson(v.conditionalToken)
    ],
    [
      "idle",
      Spice.stringToJson(v.idle)
    ],
    [
      "on_book",
      Spice.stringToJson(v.onBook)
    ]
  ]));
}

function t_encodeJson$17(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "outcome_index",
      Spice.floatToJson(v.outcomeIndex)
    ],
    [
      "conditional_token",
      Shared.pubkeyStr_encodeJson(v.conditionalToken)
    ],
    [
      "idle",
      Spice.stringToJson(v.idle)
    ],
    [
      "on_book",
      Spice.stringToJson(v.onBook)
    ]
  ]));
}

function t_decode$19(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let outcomeIndex = Stdlib_Option.getOr(Stdlib_Option.map(v["outcome_index"], Spice.floatFromJson), Spice.error(undefined, "outcome_index" + " missing", v));
  if (outcomeIndex.TAG === "Ok") {
    let conditionalToken = Stdlib_Option.getOr(Stdlib_Option.map(v["conditional_token"], Shared.pubkeyStr_decode), Spice.error(undefined, "conditional_token" + " missing", v));
    if (conditionalToken.TAG === "Ok") {
      let idle = Stdlib_Option.getOr(Stdlib_Option.map(v["idle"], Spice.stringFromJson), Spice.error(undefined, "idle" + " missing", v));
      if (idle.TAG === "Ok") {
        let onBook = Stdlib_Option.getOr(Stdlib_Option.map(v["on_book"], Spice.stringFromJson), Spice.error(undefined, "on_book" + " missing", v));
        if (onBook.TAG === "Ok") {
          return {
            TAG: "Ok",
            _0: {
              outcomeIndex: outcomeIndex._0,
              conditionalToken: conditionalToken._0,
              idle: idle._0,
              onBook: onBook._0
            }
          };
        }
        let e = onBook._0;
        return Spice.error("." + ("on_book" + e.path), e.message, e.value);
      }
      let e$1 = idle._0;
      return Spice.error("." + ("idle" + e$1.path), e$1.message, e$1.value);
    }
    let e$2 = conditionalToken._0;
    return Spice.error("." + ("conditional_token" + e$2.path), e$2.message, e$2.value);
  }
  let e$3 = outcomeIndex._0;
  return Spice.error("." + ("outcome_index" + e$3.path), e$3.message, e$3.value);
}

let ConditionalBalance = {
  t_encode: t_encode$18,
  t_encodeJson: t_encodeJson$17,
  t_decode: t_decode$19
};

function t_encode$19(v) {
  let extra = v.outcomes;
  return Object.fromEntries(Spice.filterOptional([[
      "outcomes",
      Spice.arrayToJson(t_encodeJson$17, extra)
    ]]));
}

function t_encodeJson$18(v) {
  let extra = v.outcomes;
  return Object.fromEntries(Spice.filterOptional([[
      "outcomes",
      Spice.arrayToJson(t_encodeJson$17, extra)
    ]]));
}

function t_decode$20(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let outcomes = Stdlib_Option.getOr(Stdlib_Option.map(v["outcomes"], extra => Spice.arrayFromJson(t_decode$19, extra)), Spice.error(undefined, "outcomes" + " missing", v));
  if (outcomes.TAG === "Ok") {
    return {
      TAG: "Ok",
      _0: {
        outcomes: outcomes._0
      }
    };
  }
  let e = outcomes._0;
  return Spice.error("." + ("outcomes" + e.path), e.message, e.value);
}

let UpdateBalance = {
  t_encode: t_encode$19,
  t_encodeJson: t_encodeJson$18,
  t_decode: t_decode$20
};

function t_encode$20(v) {
  let extra = v.balance;
  return Object.fromEntries(Spice.filterOptional([
    [
      "order_hash",
      Spice.stringToJson(v.orderHash)
    ],
    [
      "price",
      Spice.stringToJson(v.price)
    ],
    [
      "is_maker",
      Spice.boolToJson(v.isMaker)
    ],
    [
      "remaining",
      Spice.stringToJson(v.remaining)
    ],
    [
      "filled",
      Spice.stringToJson(v.filled)
    ],
    [
      "fill_amount",
      Spice.stringToJson(v.fillAmount)
    ],
    [
      "side",
      Shared.Side.t_encodeJson(v.side)
    ],
    [
      "created_at",
      Spice.floatToJson(v.createdAt)
    ],
    [
      "base_mint",
      Shared.pubkeyStr_encodeJson(v.baseMint)
    ],
    [
      "quote_mint",
      Shared.pubkeyStr_encodeJson(v.quoteMint)
    ],
    [
      "outcome_index",
      Spice.floatToJson(v.outcomeIndex)
    ],
    [
      "status",
      Shared.OrderStatus.t_encodeJson(v.status)
    ],
    [
      "balance",
      Spice.optionToJson(t_encodeJson$18, extra)
    ]
  ]));
}

function t_encodeJson$19(v) {
  let extra = v.balance;
  return Object.fromEntries(Spice.filterOptional([
    [
      "order_hash",
      Spice.stringToJson(v.orderHash)
    ],
    [
      "price",
      Spice.stringToJson(v.price)
    ],
    [
      "is_maker",
      Spice.boolToJson(v.isMaker)
    ],
    [
      "remaining",
      Spice.stringToJson(v.remaining)
    ],
    [
      "filled",
      Spice.stringToJson(v.filled)
    ],
    [
      "fill_amount",
      Spice.stringToJson(v.fillAmount)
    ],
    [
      "side",
      Shared.Side.t_encodeJson(v.side)
    ],
    [
      "created_at",
      Spice.floatToJson(v.createdAt)
    ],
    [
      "base_mint",
      Shared.pubkeyStr_encodeJson(v.baseMint)
    ],
    [
      "quote_mint",
      Shared.pubkeyStr_encodeJson(v.quoteMint)
    ],
    [
      "outcome_index",
      Spice.floatToJson(v.outcomeIndex)
    ],
    [
      "status",
      Shared.OrderStatus.t_encodeJson(v.status)
    ],
    [
      "balance",
      Spice.optionToJson(t_encodeJson$18, extra)
    ]
  ]));
}

function t_decode$21(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let orderHash = Stdlib_Option.getOr(Stdlib_Option.map(v["order_hash"], Spice.stringFromJson), Spice.error(undefined, "order_hash" + " missing", v));
  if (orderHash.TAG === "Ok") {
    let price = Stdlib_Option.getOr(Stdlib_Option.map(v["price"], Spice.stringFromJson), Spice.error(undefined, "price" + " missing", v));
    if (price.TAG === "Ok") {
      let isMaker = Stdlib_Option.getOr(Stdlib_Option.map(v["is_maker"], Spice.boolFromJson), Spice.error(undefined, "is_maker" + " missing", v));
      if (isMaker.TAG === "Ok") {
        let remaining = Stdlib_Option.getOr(Stdlib_Option.map(v["remaining"], Spice.stringFromJson), Spice.error(undefined, "remaining" + " missing", v));
        if (remaining.TAG === "Ok") {
          let filled = Stdlib_Option.getOr(Stdlib_Option.map(v["filled"], Spice.stringFromJson), Spice.error(undefined, "filled" + " missing", v));
          if (filled.TAG === "Ok") {
            let fillAmount = Stdlib_Option.getOr(Stdlib_Option.map(v["fill_amount"], Spice.stringFromJson), Spice.error(undefined, "fill_amount" + " missing", v));
            if (fillAmount.TAG === "Ok") {
              let side = Stdlib_Option.getOr(Stdlib_Option.map(v["side"], Shared.Side.t_decode), Spice.error(undefined, "side" + " missing", v));
              if (side.TAG === "Ok") {
                let createdAt = Stdlib_Option.getOr(Stdlib_Option.map(v["created_at"], Spice.floatFromJson), Spice.error(undefined, "created_at" + " missing", v));
                if (createdAt.TAG === "Ok") {
                  let baseMint = Stdlib_Option.getOr(Stdlib_Option.map(v["base_mint"], Shared.pubkeyStr_decode), Spice.error(undefined, "base_mint" + " missing", v));
                  if (baseMint.TAG === "Ok") {
                    let quoteMint = Stdlib_Option.getOr(Stdlib_Option.map(v["quote_mint"], Shared.pubkeyStr_decode), Spice.error(undefined, "quote_mint" + " missing", v));
                    if (quoteMint.TAG === "Ok") {
                      let outcomeIndex = Stdlib_Option.getOr(Stdlib_Option.map(v["outcome_index"], Spice.floatFromJson), Spice.error(undefined, "outcome_index" + " missing", v));
                      if (outcomeIndex.TAG === "Ok") {
                        let status = Stdlib_Option.getOr(Stdlib_Option.map(v["status"], Shared.OrderStatus.t_decode), {
                          TAG: "Ok",
                          _0: "OPEN"
                        });
                        if (status.TAG === "Ok") {
                          let balance = Stdlib_Option.getOr(Stdlib_Option.map(v["balance"], json => Stdlib_Result.map(t_decode$20(json), v => v)), {
                            TAG: "Ok",
                            _0: undefined
                          });
                          if (balance.TAG === "Ok") {
                            return {
                              TAG: "Ok",
                              _0: {
                                orderHash: orderHash._0,
                                price: price._0,
                                isMaker: isMaker._0,
                                remaining: remaining._0,
                                filled: filled._0,
                                fillAmount: fillAmount._0,
                                side: side._0,
                                createdAt: createdAt._0,
                                baseMint: baseMint._0,
                                quoteMint: quoteMint._0,
                                outcomeIndex: outcomeIndex._0,
                                status: status._0,
                                balance: balance._0
                              }
                            };
                          }
                          let e = balance._0;
                          return Spice.error("." + ("balance" + e.path), e.message, e.value);
                        }
                        let e$1 = status._0;
                        return Spice.error("." + ("status" + e$1.path), e$1.message, e$1.value);
                      }
                      let e$2 = outcomeIndex._0;
                      return Spice.error("." + ("outcome_index" + e$2.path), e$2.message, e$2.value);
                    }
                    let e$3 = quoteMint._0;
                    return Spice.error("." + ("quote_mint" + e$3.path), e$3.message, e$3.value);
                  }
                  let e$4 = baseMint._0;
                  return Spice.error("." + ("base_mint" + e$4.path), e$4.message, e$4.value);
                }
                let e$5 = createdAt._0;
                return Spice.error("." + ("created_at" + e$5.path), e$5.message, e$5.value);
              }
              let e$6 = side._0;
              return Spice.error("." + ("side" + e$6.path), e$6.message, e$6.value);
            }
            let e$7 = fillAmount._0;
            return Spice.error("." + ("fill_amount" + e$7.path), e$7.message, e$7.value);
          }
          let e$8 = filled._0;
          return Spice.error("." + ("filled" + e$8.path), e$8.message, e$8.value);
        }
        let e$9 = remaining._0;
        return Spice.error("." + ("remaining" + e$9.path), e$9.message, e$9.value);
      }
      let e$10 = isMaker._0;
      return Spice.error("." + ("is_maker" + e$10.path), e$10.message, e$10.value);
    }
    let e$11 = price._0;
    return Spice.error("." + ("price" + e$11.path), e$11.message, e$11.value);
  }
  let e$12 = orderHash._0;
  return Spice.error("." + ("order_hash" + e$12.path), e$12.message, e$12.value);
}

let WsOrder = {
  t_encode: t_encode$20,
  t_encodeJson: t_encodeJson$19,
  t_decode: t_decode$21
};

function t_encode$21(v) {
  let extra = v.txSignature;
  return Object.fromEntries(Spice.filterOptional([
    [
      "market_pubkey",
      Shared.pubkeyStr_encodeJson(v.marketPubkey)
    ],
    [
      "orderbook_id",
      Shared.orderBookId_encodeJson(v.orderbookId)
    ],
    [
      "timestamp",
      Spice.stringToJson(v.timestamp)
    ],
    [
      "tx_signature",
      Spice.optionToJson(Spice.stringToJson, extra)
    ],
    [
      "type",
      Shared.OrderUpdateType.t_encodeJson(v.updateType)
    ],
    [
      "order",
      t_encodeJson$19(v.order)
    ]
  ]));
}

function t_encodeJson$20(v) {
  let extra = v.txSignature;
  return Object.fromEntries(Spice.filterOptional([
    [
      "market_pubkey",
      Shared.pubkeyStr_encodeJson(v.marketPubkey)
    ],
    [
      "orderbook_id",
      Shared.orderBookId_encodeJson(v.orderbookId)
    ],
    [
      "timestamp",
      Spice.stringToJson(v.timestamp)
    ],
    [
      "tx_signature",
      Spice.optionToJson(Spice.stringToJson, extra)
    ],
    [
      "type",
      Shared.OrderUpdateType.t_encodeJson(v.updateType)
    ],
    [
      "order",
      t_encodeJson$19(v.order)
    ]
  ]));
}

function t_decode$22(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let marketPubkey = Stdlib_Option.getOr(Stdlib_Option.map(v["market_pubkey"], Shared.pubkeyStr_decode), Spice.error(undefined, "market_pubkey" + " missing", v));
  if (marketPubkey.TAG === "Ok") {
    let orderbookId = Stdlib_Option.getOr(Stdlib_Option.map(v["orderbook_id"], Shared.orderBookId_decode), Spice.error(undefined, "orderbook_id" + " missing", v));
    if (orderbookId.TAG === "Ok") {
      let timestamp = Stdlib_Option.getOr(Stdlib_Option.map(v["timestamp"], Spice.stringFromJson), Spice.error(undefined, "timestamp" + " missing", v));
      if (timestamp.TAG === "Ok") {
        let txSignature = Stdlib_Option.getOr(Stdlib_Option.map(v["tx_signature"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
          TAG: "Ok",
          _0: undefined
        });
        if (txSignature.TAG === "Ok") {
          let updateType = Stdlib_Option.getOr(Stdlib_Option.map(v["type"], Shared.OrderUpdateType.t_decode), {
            TAG: "Ok",
            _0: "UPDATE"
          });
          if (updateType.TAG === "Ok") {
            let order = Stdlib_Option.getOr(Stdlib_Option.map(v["order"], t_decode$21), Spice.error(undefined, "order" + " missing", v));
            if (order.TAG === "Ok") {
              return {
                TAG: "Ok",
                _0: {
                  marketPubkey: marketPubkey._0,
                  orderbookId: orderbookId._0,
                  timestamp: timestamp._0,
                  txSignature: txSignature._0,
                  updateType: updateType._0,
                  order: order._0
                }
              };
            }
            let e = order._0;
            return Spice.error("." + ("order" + e.path), e.message, e.value);
          }
          let e$1 = updateType._0;
          return Spice.error("." + ("type" + e$1.path), e$1.message, e$1.value);
        }
        let e$2 = txSignature._0;
        return Spice.error("." + ("tx_signature" + e$2.path), e$2.message, e$2.value);
      }
      let e$3 = timestamp._0;
      return Spice.error("." + ("timestamp" + e$3.path), e$3.message, e$3.value);
    }
    let e$4 = orderbookId._0;
    return Spice.error("." + ("orderbook_id" + e$4.path), e$4.message, e$4.value);
  }
  let e$5 = marketPubkey._0;
  return Spice.error("." + ("market_pubkey" + e$5.path), e$5.message, e$5.value);
}

function toLimit$1(update) {
  return {
    marketPubkey: update.marketPubkey,
    orderbookId: update.orderbookId,
    txSignature: update.txSignature,
    baseMint: update.order.baseMint,
    quoteMint: update.order.quoteMint,
    orderHash: update.order.orderHash,
    side: update.order.side,
    size: sizeOf(update.order.filled, update.order.remaining),
    price: update.order.price,
    filledSize: update.order.filled,
    remainingSize: update.order.remaining,
    createdAt: update.order.createdAt,
    status: update.order.status,
    outcomeIndex: update.order.outcomeIndex
  };
}

let Update = {
  t_encode: t_encode$21,
  t_encodeJson: t_encodeJson$20,
  t_decode: t_decode$22,
  toLimit: toLimit$1
};

function updateTypeDecode(dict) {
  let json = dict["type"];
  if (json !== undefined) {
    return Shared.TriggerUpdateType.t_decode(json);
  } else {
    return {
      TAG: "Ok",
      _0: "TRIGGERED"
    };
  }
}

function resultStatusDecode(dict) {
  let json = dict["result_status"];
  if (json === undefined) {
    return {
      TAG: "Ok",
      _0: undefined
    };
  }
  if (json === "") {
    return {
      TAG: "Ok",
      _0: undefined
    };
  }
  return Stdlib_Result.map(Shared.TriggerResultStatus.t_decode(json), value => value);
}

function t_decode$23(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "trigger order update is not an object", json);
  }
  let match = optString(json, "trigger_order_id");
  let match$1 = optString(json, "market_pubkey");
  let match$2 = optString(json, "orderbook_id");
  let match$3 = optString(json, "trigger_price");
  let match$4 = json["trigger_above"];
  let match$5 = json["status"];
  let match$6 = optString(json, "order_hash");
  let match$7 = json["side"];
  let match$8 = optString(json, "timestamp");
  if (match === undefined) {
    return Spice.error(undefined, "trigger order update missing required fields", json);
  }
  if (match$1 === undefined) {
    return Spice.error(undefined, "trigger order update missing required fields", json);
  }
  if (match$2 === undefined) {
    return Spice.error(undefined, "trigger order update missing required fields", json);
  }
  if (match$3 === undefined) {
    return Spice.error(undefined, "trigger order update missing required fields", json);
  }
  if (match$4 === undefined) {
    return Spice.error(undefined, "trigger order update missing required fields", json);
  }
  if (typeof match$4 !== "boolean") {
    return Spice.error(undefined, "trigger order update missing required fields", json);
  }
  if (match$5 === undefined) {
    return Spice.error(undefined, "trigger order update missing required fields", json);
  }
  if (match$6 === undefined) {
    return Spice.error(undefined, "trigger order update missing required fields", json);
  }
  if (match$7 === undefined) {
    return Spice.error(undefined, "trigger order update missing required fields", json);
  }
  if (match$8 === undefined) {
    return Spice.error(undefined, "trigger order update missing required fields", json);
  }
  let match$9 = Shared.TriggerStatus.t_decode(match$5);
  let match$10 = Shared.Side.t_decode(match$7);
  let match$11 = updateTypeDecode(json);
  let match$12 = resultStatusDecode(json);
  let match$13 = tifNumericDecode(json, "tif", json);
  if (match$9.TAG === "Ok") {
    if (match$10.TAG === "Ok") {
      if (match$11.TAG === "Ok") {
        if (match$12.TAG === "Ok") {
          if (match$13.TAG === "Ok") {
            return {
              TAG: "Ok",
              _0: {
                triggerOrderId: match,
                userPubkey: Stdlib_Option.getOr(optString(json, "user_pubkey"), ""),
                marketPubkey: match$1,
                orderbookId: match$2,
                triggerPrice: match$3,
                triggerAbove: match$4,
                status: match$9._0,
                updateType: match$11._0,
                orderHash: match$6,
                side: match$10._0,
                resultStatus: match$12._0,
                resultFilled: decimalOr0(json, "result_filled"),
                resultRemaining: decimalOr0(json, "result_remaining"),
                timestamp: match$8,
                makerAmount: decimalOr0(json, "maker_amount"),
                takerAmount: decimalOr0(json, "taker_amount"),
                tif: match$13._0
              }
            };
          } else {
            return {
              TAG: "Error",
              _0: match$13._0
            };
          }
        } else {
          return {
            TAG: "Error",
            _0: match$12._0
          };
        }
      } else {
        return {
          TAG: "Error",
          _0: match$11._0
        };
      }
    } else {
      return {
        TAG: "Error",
        _0: match$10._0
      };
    }
  } else {
    return {
      TAG: "Error",
      _0: match$9._0
    };
  }
}

function toTrigger$1(update) {
  return {
    triggerOrderId: update.triggerOrderId,
    orderHash: update.orderHash,
    marketPubkey: update.marketPubkey,
    orderbookId: update.orderbookId,
    triggerPrice: update.triggerPrice,
    triggerType: update.triggerAbove ? "TP" : "SL",
    side: update.side,
    amountIn: update.makerAmount,
    amountOut: update.takerAmount,
    timeInForce: update.tif,
    createdAt: new Date(update.timestamp).getTime()
  };
}

function t_decode$24(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "order event is not an object", json);
  }
  let match = json["order_type"];
  if (match === undefined) {
    return Spice.error(undefined, "unknown order event order_type", json);
  }
  if (typeof match !== "string") {
    return Spice.error(undefined, "unknown order event order_type", json);
  }
  switch (match) {
    case "limit" :
      return Stdlib_Result.map(t_decode$22(json), value => ({
        TAG: "Limit",
        _0: value
      }));
    case "trigger" :
      return Stdlib_Result.map(t_decode$23(json), value => ({
        TAG: "Trigger",
        _0: value
      }));
    default:
      return Spice.error(undefined, "unknown order event order_type", json);
  }
}

let Event = {
  t_decode: t_decode$24
};

function t_decode$25(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "user snapshot is not an object", json);
  }
  let match = json["orders"];
  let match$1 = json["market_balances"];
  if (match === undefined) {
    return Spice.error(undefined, "user snapshot missing orders/market_balances", json);
  }
  if (match$1 === undefined) {
    return Spice.error(undefined, "user snapshot missing orders/market_balances", json);
  }
  let match$2 = Spice.arrayFromJson(t_decode$8, match);
  let match$3 = Spice.arrayFromJson(t_decode$11, match$1);
  let match$4 = Stdlib_Option.mapOr(json["global_deposits"], {
    TAG: "Ok",
    _0: []
  }, json => Spice.arrayFromJson(t_decode$12, json));
  let match$5 = Stdlib_Option.mapOr(json["notifications"], {
    TAG: "Ok",
    _0: []
  }, json => Spice.arrayFromJson(Notification__Raw.decode, json));
  if (match$2.TAG === "Ok") {
    if (match$3.TAG === "Ok") {
      if (match$4.TAG === "Ok") {
        if (match$5.TAG === "Ok") {
          return {
            TAG: "Ok",
            _0: {
              orders: match$2._0,
              marketBalances: match$3._0,
              globalDeposits: match$4._0,
              notifications: match$5._0,
              nonce: Stdlib_Option.getOr(optFloat(json, "nonce"), 0.0)
            }
          };
        } else {
          return {
            TAG: "Error",
            _0: match$5._0
          };
        }
      } else {
        return {
          TAG: "Error",
          _0: match$4._0
        };
      }
    } else {
      return {
        TAG: "Error",
        _0: match$3._0
      };
    }
  } else {
    return {
      TAG: "Error",
      _0: match$2._0
    };
  }
}

let UserSnapshot = {
  t_decode: t_decode$25
};

let SnapshotCommon = {};

let TriggerUpdate = {
  t_decode: t_decode$23,
  toTrigger: toTrigger$1
};

export {
  FillInfo,
  SubmitStatus,
  SubmitResponse,
  SubmitRequest,
  TriggerResponse,
  CancelSuccess,
  CancelAllSuccess,
  CancelTriggerSuccess,
  CancelBody,
  CancelAllBody,
  CancelTriggerBody,
  SnapshotCommon,
  SnapshotLimit,
  SnapshotTrigger,
  SnapshotOrder,
  UserOutcomeBalance,
  UserDepositAssetBalance,
  UserMarketBalance,
  GlobalDepositBalance,
  UserOrdersResponse,
  Role,
  FillStatus,
  FillEvent,
  UserFill,
  UserFillsResponse,
  ConditionalBalance,
  UpdateBalance,
  WsOrder,
  Update,
  TriggerUpdate,
  Event,
  UserSnapshot,
}
/* Shared Not a pure module */
