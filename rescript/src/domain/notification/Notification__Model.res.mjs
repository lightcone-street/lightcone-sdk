// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Spice from "@mununki/ppx-spice/src/rescript/Spice.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

function t_encode(v) {
  if (v === "single_winner") {
    return "single_winner";
  } else {
    return "scalar";
  }
}

function t_encodeJson(v) {
  if (v === "single_winner") {
    return "single_winner";
  } else {
    return "scalar";
  }
}

function t_decode(v) {
  switch (typeof v) {
    case "string" :
      if ("single_winner" === v) {
        return {
          TAG: "Ok",
          _0: "single_winner"
        };
      } else if ("scalar" === v) {
        return {
          TAG: "Ok",
          _0: "scalar"
        };
      } else {
        return Spice.error(undefined, "Not matched", v);
      }
    case "number" :
      return Spice.error(undefined, "Not matched", v);
    default:
      return Spice.error(undefined, "Not a JSONString", v);
  }
}

let Kind = {
  t_encode: t_encode,
  t_encodeJson: t_encodeJson,
  t_decode: t_decode
};

function payout_encode(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "outcome_index",
      Spice.floatToJson(v.outcomeIndex)
    ],
    [
      "payout_numerator",
      Spice.floatToJson(v.payoutNumerator)
    ]
  ]));
}

function payout_encodeJson(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "outcome_index",
      Spice.floatToJson(v.outcomeIndex)
    ],
    [
      "payout_numerator",
      Spice.floatToJson(v.payoutNumerator)
    ]
  ]));
}

function payout_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let outcomeIndex = Stdlib_Option.getOr(Stdlib_Option.map(v["outcome_index"], Spice.floatFromJson), Spice.error(undefined, "outcome_index" + " missing", v));
  if (outcomeIndex.TAG === "Ok") {
    let payoutNumerator = Stdlib_Option.getOr(Stdlib_Option.map(v["payout_numerator"], Spice.floatFromJson), Spice.error(undefined, "payout_numerator" + " missing", v));
    if (payoutNumerator.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: {
          outcomeIndex: outcomeIndex._0,
          payoutNumerator: payoutNumerator._0
        }
      };
    }
    let e = payoutNumerator._0;
    return Spice.error("." + ("payout_numerator" + e.path), e.message, e.value);
  }
  let e$1 = outcomeIndex._0;
  return Spice.error("." + ("outcome_index" + e$1.path), e$1.message, e$1.value);
}

let MarketResolution = {
  Kind: Kind,
  payout_encode: payout_encode,
  payout_encodeJson: payout_encodeJson,
  payout_decode: payout_decode
};

let MarketResolved = {};

let OrderFilled = {};

let MarketData = {};

let Kind$1 = {};

function isGlobal(notification) {
  let match = notification.kind;
  return typeof match !== "object";
}

function marketSlug(notification) {
  let data = notification.kind;
  if (typeof data !== "object") {
    return;
  }
  switch (data.TAG) {
    case "OrderFilled" :
      return data._0.marketSlug;
    case "MarketResolved" :
    case "NewMarket" :
    case "RulesClarified" :
      return data._0.marketSlug;
  }
}

export {
  MarketResolution,
  MarketResolved,
  OrderFilled,
  MarketData,
  Kind$1 as Kind,
  isGlobal,
  marketSlug,
}
/* No side effect */
