// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Spice from "@mununki/ppx-spice/src/rescript/Spice.res.mjs";
import * as Shared from "../../Shared.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Stdlib_Result from "@rescript/runtime/lib/es6/Stdlib_Result.js";
import * as Orderbook__Model from "./Orderbook__Model.res.mjs";

function t_encode(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "side",
      Shared.Side.t_encodeJson(v.side)
    ],
    [
      "price",
      Spice.stringToJson(v.price)
    ],
    [
      "size",
      Spice.stringToJson(v.size)
    ]
  ]));
}

function t_encodeJson(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "side",
      Shared.Side.t_encodeJson(v.side)
    ],
    [
      "price",
      Spice.stringToJson(v.price)
    ],
    [
      "size",
      Spice.stringToJson(v.size)
    ]
  ]));
}

function t_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let side = Stdlib_Option.getOr(Stdlib_Option.map(v["side"], Shared.Side.t_decode), Spice.error(undefined, "side" + " missing", v));
  if (side.TAG === "Ok") {
    let price = Stdlib_Option.getOr(Stdlib_Option.map(v["price"], Spice.stringFromJson), Spice.error(undefined, "price" + " missing", v));
    if (price.TAG === "Ok") {
      let size = Stdlib_Option.getOr(Stdlib_Option.map(v["size"], Spice.stringFromJson), Spice.error(undefined, "size" + " missing", v));
      if (size.TAG === "Ok") {
        return {
          TAG: "Ok",
          _0: {
            side: side._0,
            price: price._0,
            size: size._0
          }
        };
      }
      let e = size._0;
      return Spice.error("." + ("size" + e.path), e.message, e.value);
    }
    let e$1 = price._0;
    return Spice.error("." + ("price" + e$1.path), e$1.message, e$1.value);
  }
  let e$2 = side._0;
  return Spice.error("." + ("side" + e$2.path), e$2.message, e$2.value);
}

let WsLevel = {
  t_encode: t_encode,
  t_encodeJson: t_encodeJson,
  t_decode: t_decode
};

function t_encode$1(v) {
  let extra = v.orders;
  return Object.fromEntries(Spice.filterOptional([
    [
      "price",
      Spice.stringToJson(v.price)
    ],
    [
      "size",
      Spice.stringToJson(v.size)
    ],
    [
      "orders",
      Spice.optionToJson(Spice.floatToJson, extra)
    ]
  ]));
}

function t_encodeJson$1(v) {
  let extra = v.orders;
  return Object.fromEntries(Spice.filterOptional([
    [
      "price",
      Spice.stringToJson(v.price)
    ],
    [
      "size",
      Spice.stringToJson(v.size)
    ],
    [
      "orders",
      Spice.optionToJson(Spice.floatToJson, extra)
    ]
  ]));
}

function t_decode$1(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let price = Stdlib_Option.getOr(Stdlib_Option.map(v["price"], Spice.stringFromJson), Spice.error(undefined, "price" + " missing", v));
  if (price.TAG === "Ok") {
    let size = Stdlib_Option.getOr(Stdlib_Option.map(v["size"], Spice.stringFromJson), Spice.error(undefined, "size" + " missing", v));
    if (size.TAG === "Ok") {
      let orders = Stdlib_Option.getOr(Stdlib_Option.map(v["orders"], json => Stdlib_Result.map(Spice.floatFromJson(json), v => v)), {
        TAG: "Ok",
        _0: undefined
      });
      if (orders.TAG === "Ok") {
        return {
          TAG: "Ok",
          _0: {
            price: price._0,
            size: size._0,
            orders: orders._0
          }
        };
      }
      let e = orders._0;
      return Spice.error("." + ("orders" + e.path), e.message, e.value);
    }
    let e$1 = size._0;
    return Spice.error("." + ("size" + e$1.path), e$1.message, e$1.value);
  }
  let e$2 = price._0;
  return Spice.error("." + ("price" + e$2.path), e$2.message, e$2.value);
}

let RestLevel = {
  t_encode: t_encode$1,
  t_encodeJson: t_encodeJson$1,
  t_decode: t_decode$1
};

function t_encode$2(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "price",
      Spice.floatToJson(v.price)
    ],
    [
      "size",
      Spice.floatToJson(v.size)
    ]
  ]));
}

function t_encodeJson$2(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "price",
      Spice.floatToJson(v.price)
    ],
    [
      "size",
      Spice.floatToJson(v.size)
    ]
  ]));
}

function t_decode$2(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let price = Stdlib_Option.getOr(Stdlib_Option.map(v["price"], Spice.floatFromJson), Spice.error(undefined, "price" + " missing", v));
  if (price.TAG === "Ok") {
    let size = Stdlib_Option.getOr(Stdlib_Option.map(v["size"], Spice.floatFromJson), Spice.error(undefined, "size" + " missing", v));
    if (size.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: {
          price: price._0,
          size: size._0
        }
      };
    }
    let e = size._0;
    return Spice.error("." + ("size" + e.path), e.message, e.value);
  }
  let e$1 = price._0;
  return Spice.error("." + ("price" + e$1.path), e$1.message, e$1.value);
}

let DepthDecimals = {
  t_encode: t_encode$2,
  t_encodeJson: t_encodeJson$2,
  t_decode: t_decode$2
};

function t_encode$3(v) {
  let extra = v.marketPubkey;
  let extra$1 = v.bestBid;
  let extra$2 = v.bestAsk;
  let extra$3 = v.spread;
  let extra$4 = v.tickSize;
  let extra$5 = v.bids;
  let extra$6 = v.asks;
  let extra$7 = v.decimals;
  return Object.fromEntries(Spice.filterOptional([
    [
      "orderbook_id",
      Shared.orderBookId_encodeJson(v.orderbookId)
    ],
    [
      "market_pubkey",
      Spice.optionToJson(Spice.stringToJson, extra)
    ],
    [
      "best_bid",
      Spice.optionToJson(Spice.stringToJson, extra$1)
    ],
    [
      "best_ask",
      Spice.optionToJson(Spice.stringToJson, extra$2)
    ],
    [
      "spread",
      Spice.optionToJson(Spice.stringToJson, extra$3)
    ],
    [
      "tick_size",
      Spice.optionToJson(Spice.stringToJson, extra$4)
    ],
    [
      "bids",
      Spice.arrayToJson(t_encodeJson$1, extra$5)
    ],
    [
      "asks",
      Spice.arrayToJson(t_encodeJson$1, extra$6)
    ],
    [
      "decimals",
      Spice.optionToJson(t_encodeJson$2, extra$7)
    ]
  ]));
}

function t_encodeJson$3(v) {
  let extra = v.marketPubkey;
  let extra$1 = v.bestBid;
  let extra$2 = v.bestAsk;
  let extra$3 = v.spread;
  let extra$4 = v.tickSize;
  let extra$5 = v.bids;
  let extra$6 = v.asks;
  let extra$7 = v.decimals;
  return Object.fromEntries(Spice.filterOptional([
    [
      "orderbook_id",
      Shared.orderBookId_encodeJson(v.orderbookId)
    ],
    [
      "market_pubkey",
      Spice.optionToJson(Spice.stringToJson, extra)
    ],
    [
      "best_bid",
      Spice.optionToJson(Spice.stringToJson, extra$1)
    ],
    [
      "best_ask",
      Spice.optionToJson(Spice.stringToJson, extra$2)
    ],
    [
      "spread",
      Spice.optionToJson(Spice.stringToJson, extra$3)
    ],
    [
      "tick_size",
      Spice.optionToJson(Spice.stringToJson, extra$4)
    ],
    [
      "bids",
      Spice.arrayToJson(t_encodeJson$1, extra$5)
    ],
    [
      "asks",
      Spice.arrayToJson(t_encodeJson$1, extra$6)
    ],
    [
      "decimals",
      Spice.optionToJson(t_encodeJson$2, extra$7)
    ]
  ]));
}

function t_decode$3(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let orderbookId = Stdlib_Option.getOr(Stdlib_Option.map(v["orderbook_id"], Shared.orderBookId_decode), Spice.error(undefined, "orderbook_id" + " missing", v));
  if (orderbookId.TAG === "Ok") {
    let marketPubkey = Stdlib_Option.getOr(Stdlib_Option.map(v["market_pubkey"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
      TAG: "Ok",
      _0: undefined
    });
    if (marketPubkey.TAG === "Ok") {
      let bestBid = Stdlib_Option.getOr(Stdlib_Option.map(v["best_bid"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
        TAG: "Ok",
        _0: undefined
      });
      if (bestBid.TAG === "Ok") {
        let bestAsk = Stdlib_Option.getOr(Stdlib_Option.map(v["best_ask"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
          TAG: "Ok",
          _0: undefined
        });
        if (bestAsk.TAG === "Ok") {
          let spread = Stdlib_Option.getOr(Stdlib_Option.map(v["spread"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
            TAG: "Ok",
            _0: undefined
          });
          if (spread.TAG === "Ok") {
            let tickSize = Stdlib_Option.getOr(Stdlib_Option.map(v["tick_size"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
              TAG: "Ok",
              _0: undefined
            });
            if (tickSize.TAG === "Ok") {
              let bids = Stdlib_Option.getOr(Stdlib_Option.map(v["bids"], extra => Spice.arrayFromJson(t_decode$1, extra)), Spice.error(undefined, "bids" + " missing", v));
              if (bids.TAG === "Ok") {
                let asks = Stdlib_Option.getOr(Stdlib_Option.map(v["asks"], extra => Spice.arrayFromJson(t_decode$1, extra)), Spice.error(undefined, "asks" + " missing", v));
                if (asks.TAG === "Ok") {
                  let decimals = Stdlib_Option.getOr(Stdlib_Option.map(v["decimals"], json => Stdlib_Result.map(t_decode$2(json), v => v)), {
                    TAG: "Ok",
                    _0: undefined
                  });
                  if (decimals.TAG === "Ok") {
                    return {
                      TAG: "Ok",
                      _0: {
                        orderbookId: orderbookId._0,
                        marketPubkey: marketPubkey._0,
                        bestBid: bestBid._0,
                        bestAsk: bestAsk._0,
                        spread: spread._0,
                        tickSize: tickSize._0,
                        bids: bids._0,
                        asks: asks._0,
                        decimals: decimals._0
                      }
                    };
                  }
                  let e = decimals._0;
                  return Spice.error("." + ("decimals" + e.path), e.message, e.value);
                }
                let e$1 = asks._0;
                return Spice.error("." + ("asks" + e$1.path), e$1.message, e$1.value);
              }
              let e$2 = bids._0;
              return Spice.error("." + ("bids" + e$2.path), e$2.message, e$2.value);
            }
            let e$3 = tickSize._0;
            return Spice.error("." + ("tick_size" + e$3.path), e$3.message, e$3.value);
          }
          let e$4 = spread._0;
          return Spice.error("." + ("spread" + e$4.path), e$4.message, e$4.value);
        }
        let e$5 = bestAsk._0;
        return Spice.error("." + ("best_ask" + e$5.path), e$5.message, e$5.value);
      }
      let e$6 = bestBid._0;
      return Spice.error("." + ("best_bid" + e$6.path), e$6.message, e$6.value);
    }
    let e$7 = marketPubkey._0;
    return Spice.error("." + ("market_pubkey" + e$7.path), e$7.message, e$7.value);
  }
  let e$8 = orderbookId._0;
  return Spice.error("." + ("orderbook_id" + e$8.path), e$8.message, e$8.value);
}

let DepthResponse = {
  t_encode: t_encode$3,
  t_encodeJson: t_encodeJson$3,
  t_decode: t_decode$3
};

function t_encode$4(v) {
  let extra = v.bids;
  let extra$1 = v.asks;
  let extra$2 = v.nSigFigs;
  let extra$3 = v.mantissa;
  return Object.fromEntries(Spice.filterOptional([
    [
      "orderbook_id",
      Shared.orderBookId_encodeJson(v.id)
    ],
    [
      "is_snapshot",
      Spice.boolToJson(v.isSnapshot)
    ],
    [
      "seq",
      Spice.floatToJson(v.seq)
    ],
    [
      "resync",
      Spice.boolToJson(v.resync)
    ],
    [
      "bids",
      Spice.arrayToJson(t_encodeJson, extra)
    ],
    [
      "asks",
      Spice.arrayToJson(t_encodeJson, extra$1)
    ],
    [
      "n_sig_figs",
      Spice.optionToJson(Spice.intToJson, extra$2)
    ],
    [
      "mantissa",
      Spice.optionToJson(Spice.intToJson, extra$3)
    ]
  ]));
}

function t_encodeJson$4(v) {
  let extra = v.bids;
  let extra$1 = v.asks;
  let extra$2 = v.nSigFigs;
  let extra$3 = v.mantissa;
  return Object.fromEntries(Spice.filterOptional([
    [
      "orderbook_id",
      Shared.orderBookId_encodeJson(v.id)
    ],
    [
      "is_snapshot",
      Spice.boolToJson(v.isSnapshot)
    ],
    [
      "seq",
      Spice.floatToJson(v.seq)
    ],
    [
      "resync",
      Spice.boolToJson(v.resync)
    ],
    [
      "bids",
      Spice.arrayToJson(t_encodeJson, extra)
    ],
    [
      "asks",
      Spice.arrayToJson(t_encodeJson, extra$1)
    ],
    [
      "n_sig_figs",
      Spice.optionToJson(Spice.intToJson, extra$2)
    ],
    [
      "mantissa",
      Spice.optionToJson(Spice.intToJson, extra$3)
    ]
  ]));
}

function t_decode$4(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let id = Stdlib_Option.getOr(Stdlib_Option.map(v["orderbook_id"], Shared.orderBookId_decode), Spice.error(undefined, "orderbook_id" + " missing", v));
  if (id.TAG === "Ok") {
    let isSnapshot = Stdlib_Option.getOr(Stdlib_Option.map(v["is_snapshot"], Spice.boolFromJson), {
      TAG: "Ok",
      _0: false
    });
    if (isSnapshot.TAG === "Ok") {
      let seq = Stdlib_Option.getOr(Stdlib_Option.map(v["seq"], Spice.floatFromJson), {
        TAG: "Ok",
        _0: 0.0
      });
      if (seq.TAG === "Ok") {
        let resync = Stdlib_Option.getOr(Stdlib_Option.map(v["resync"], Spice.boolFromJson), {
          TAG: "Ok",
          _0: false
        });
        if (resync.TAG === "Ok") {
          let bids = Stdlib_Option.getOr(Stdlib_Option.map(v["bids"], extra => Spice.arrayFromJson(t_decode, extra)), {
            TAG: "Ok",
            _0: []
          });
          if (bids.TAG === "Ok") {
            let asks = Stdlib_Option.getOr(Stdlib_Option.map(v["asks"], extra => Spice.arrayFromJson(t_decode, extra)), {
              TAG: "Ok",
              _0: []
            });
            if (asks.TAG === "Ok") {
              let nSigFigs = Stdlib_Option.getOr(Stdlib_Option.map(v["n_sig_figs"], json => Stdlib_Result.map(Spice.intFromJson(json), v => v)), {
                TAG: "Ok",
                _0: undefined
              });
              if (nSigFigs.TAG === "Ok") {
                let mantissa = Stdlib_Option.getOr(Stdlib_Option.map(v["mantissa"], json => Stdlib_Result.map(Spice.intFromJson(json), v => v)), {
                  TAG: "Ok",
                  _0: undefined
                });
                if (mantissa.TAG === "Ok") {
                  return {
                    TAG: "Ok",
                    _0: {
                      id: id._0,
                      isSnapshot: isSnapshot._0,
                      seq: seq._0,
                      resync: resync._0,
                      bids: bids._0,
                      asks: asks._0,
                      nSigFigs: nSigFigs._0,
                      mantissa: mantissa._0
                    }
                  };
                }
                let e = mantissa._0;
                return Spice.error("." + ("mantissa" + e.path), e.message, e.value);
              }
              let e$1 = nSigFigs._0;
              return Spice.error("." + ("n_sig_figs" + e$1.path), e$1.message, e$1.value);
            }
            let e$2 = asks._0;
            return Spice.error("." + ("asks" + e$2.path), e$2.message, e$2.value);
          }
          let e$3 = bids._0;
          return Spice.error("." + ("bids" + e$3.path), e$3.message, e$3.value);
        }
        let e$4 = resync._0;
        return Spice.error("." + ("resync" + e$4.path), e$4.message, e$4.value);
      }
      let e$5 = seq._0;
      return Spice.error("." + ("seq" + e$5.path), e$5.message, e$5.value);
    }
    let e$6 = isSnapshot._0;
    return Spice.error("." + ("is_snapshot" + e$6.path), e$6.message, e$6.value);
  }
  let e$7 = id._0;
  return Spice.error("." + ("orderbook_id" + e$7.path), e$7.message, e$7.value);
}

function toAggregation(book) {
  return Orderbook__Model.Aggregation.fromFrame(book.nSigFigs, book.mantissa);
}

let Book = {
  t_encode: t_encode$4,
  t_encodeJson: t_encodeJson$4,
  t_decode: t_decode$4,
  toAggregation: toAggregation
};

export {
  WsLevel,
  RestLevel,
  DepthDecimals,
  DepthResponse,
  Book,
}
/* Shared Not a pure module */
