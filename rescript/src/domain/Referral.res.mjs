// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Http from "../Http.res.mjs";
import * as Spice from "@mununki/ppx-spice/src/rescript/Spice.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Stdlib_Result from "@rescript/runtime/lib/es6/Stdlib_Result.js";

function referralCodeWire_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let code = Stdlib_Option.getOr(Stdlib_Option.map(v["code"], Spice.stringFromJson), Spice.error(undefined, "code" + " missing", v));
  if (code.TAG === "Ok") {
    let maxUses = Stdlib_Option.getOr(Stdlib_Option.map(v["max_uses"], Spice.floatFromJson), Spice.error(undefined, "max_uses" + " missing", v));
    if (maxUses.TAG === "Ok") {
      let useCount = Stdlib_Option.getOr(Stdlib_Option.map(v["use_count"], Spice.floatFromJson), Spice.error(undefined, "use_count" + " missing", v));
      if (useCount.TAG === "Ok") {
        return {
          TAG: "Ok",
          _0: {
            code: code._0,
            maxUses: maxUses._0,
            useCount: useCount._0
          }
        };
      }
      let e = useCount._0;
      return Spice.error("." + ("use_count" + e.path), e.message, e.value);
    }
    let e$1 = maxUses._0;
    return Spice.error("." + ("max_uses" + e$1.path), e$1.message, e$1.value);
  }
  let e$2 = code._0;
  return Spice.error("." + ("code" + e$2.path), e$2.message, e$2.value);
}

function redeemResponse_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let success = Stdlib_Option.getOr(Stdlib_Option.map(v["success"], Spice.boolFromJson), Spice.error(undefined, "success" + " missing", v));
  if (success.TAG === "Ok") {
    let isBeta = Stdlib_Option.getOr(Stdlib_Option.map(v["is_beta"], Spice.boolFromJson), Spice.error(undefined, "is_beta" + " missing", v));
    if (isBeta.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: {
          success: success._0,
          isBeta: isBeta._0
        }
      };
    }
    let e = isBeta._0;
    return Spice.error("." + ("is_beta" + e.path), e.message, e.value);
  }
  let e$1 = success._0;
  return Spice.error("." + ("success" + e$1.path), e$1.message, e$1.value);
}

function referralStatusResponseDecode(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "referral status is not an object", json);
  }
  let match = json["is_beta"];
  let match$1 = json["referral_codes"];
  if (match === undefined) {
    return Spice.error(undefined, "referral status missing is_beta/referral_codes", json);
  }
  if (typeof match !== "boolean") {
    return Spice.error(undefined, "referral status missing is_beta/referral_codes", json);
  }
  if (match$1 === undefined) {
    return Spice.error(undefined, "referral status missing is_beta/referral_codes", json);
  }
  let referralCodes = Spice.arrayFromJson(referralCodeWire_decode, match$1);
  if (referralCodes.TAG !== "Ok") {
    return {
      TAG: "Error",
      _0: referralCodes._0
    };
  }
  let match$2 = json["source"];
  let source = typeof match$2 === "string" ? match$2 : undefined;
  return {
    TAG: "Ok",
    _0: {
      isBeta: match,
      source: source,
      referralCodes: referralCodes._0
    }
  };
}

function referralCodeInfoOfWire(wire) {
  return {
    code: wire.code,
    maxUses: wire.maxUses,
    useCount: wire.useCount
  };
}

function referralStatusOfResponse(response) {
  return {
    isBeta: response.isBeta,
    source: response.source,
    referralCodes: response.referralCodes.map(referralCodeInfoOfWire)
  };
}

function redeemResultOfResponse(response) {
  return {
    success: response.success,
    isBeta: response.isBeta
  };
}

async function getStatus(client, cookieHeader) {
  return Stdlib_Result.map(await Http.get(client.http, "/api/referral/status", undefined, undefined, cookieHeader, referralStatusResponseDecode), referralStatusOfResponse);
}

async function redeem(client, code) {
  let body = Object.fromEntries([[
      "code",
      code
    ]]);
  return Stdlib_Result.map(await Http.post(client.http, "/api/referral/redeem", body, undefined, undefined, redeemResponse_decode), redeemResultOfResponse);
}

export {
  getStatus,
  redeem,
}
/* Http Not a pure module */
