// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Spice from "@mununki/ppx-spice/src/rescript/Spice.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";

function t_encode(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "symbol",
      Spice.stringToJson(v.symbol)
    ],
    [
      "amount",
      Spice.floatToJson(v.amount)
    ]
  ]));
}

function t_encodeJson(v) {
  return Object.fromEntries(Spice.filterOptional([
    [
      "symbol",
      Spice.stringToJson(v.symbol)
    ],
    [
      "amount",
      Spice.floatToJson(v.amount)
    ]
  ]));
}

function t_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let symbol = Stdlib_Option.getOr(Stdlib_Option.map(v["symbol"], Spice.stringFromJson), Spice.error(undefined, "symbol" + " missing", v));
  if (symbol.TAG === "Ok") {
    let amount = Stdlib_Option.getOr(Stdlib_Option.map(v["amount"], Spice.floatFromJson), Spice.error(undefined, "amount" + " missing", v));
    if (amount.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: {
          symbol: symbol._0,
          amount: amount._0
        }
      };
    }
    let e = amount._0;
    return Spice.error("." + ("amount" + e.path), e.message, e.value);
  }
  let e$1 = symbol._0;
  return Spice.error("." + ("symbol" + e$1.path), e$1.message, e$1.value);
}

let Token = {
  t_encode: t_encode,
  t_encodeJson: t_encodeJson,
  t_decode: t_decode
};

function t_encode$1(v) {
  let extra = v.tokens;
  return Object.fromEntries(Spice.filterOptional([
    [
      "signature",
      Spice.stringToJson(v.signature)
    ],
    [
      "sol",
      Spice.floatToJson(v.sol)
    ],
    [
      "tokens",
      Spice.arrayToJson(t_encodeJson, extra)
    ]
  ]));
}

function t_encodeJson$1(v) {
  let extra = v.tokens;
  return Object.fromEntries(Spice.filterOptional([
    [
      "signature",
      Spice.stringToJson(v.signature)
    ],
    [
      "sol",
      Spice.floatToJson(v.sol)
    ],
    [
      "tokens",
      Spice.arrayToJson(t_encodeJson, extra)
    ]
  ]));
}

function t_decode$1(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let signature = Stdlib_Option.getOr(Stdlib_Option.map(v["signature"], Spice.stringFromJson), Spice.error(undefined, "signature" + " missing", v));
  if (signature.TAG === "Ok") {
    let sol = Stdlib_Option.getOr(Stdlib_Option.map(v["sol"], Spice.floatFromJson), Spice.error(undefined, "sol" + " missing", v));
    if (sol.TAG === "Ok") {
      let tokens = Stdlib_Option.getOr(Stdlib_Option.map(v["tokens"], extra => Spice.arrayFromJson(t_decode, extra)), Spice.error(undefined, "tokens" + " missing", v));
      if (tokens.TAG === "Ok") {
        return {
          TAG: "Ok",
          _0: {
            signature: signature._0,
            sol: sol._0,
            tokens: tokens._0
          }
        };
      }
      let e = tokens._0;
      return Spice.error("." + ("tokens" + e.path), e.message, e.value);
    }
    let e$1 = sol._0;
    return Spice.error("." + ("sol" + e$1.path), e$1.message, e$1.value);
  }
  let e$2 = signature._0;
  return Spice.error("." + ("signature" + e$2.path), e$2.message, e$2.value);
}

let Response = {
  t_encode: t_encode$1,
  t_encodeJson: t_encodeJson$1,
  t_decode: t_decode$1
};

export {
  Token,
  Response,
}
/* No side effect */
