// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Http from "../../Http.res.mjs";
import * as Trade__Raw from "./Trade__Raw.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Stdlib_Result from "@rescript/runtime/lib/es6/Stdlib_Result.js";

function optionalQuery(query, key, value) {
  Stdlib_Option.forEach(value, value => {
    query.push([
      key,
      value
    ]);
  });
}

async function get(client, orderbookId, limit, cursor) {
  let query = [[
      "orderbook_id",
      orderbookId
    ]];
  optionalQuery(query, "limit", Stdlib_Option.map(limit, value => value.toString()));
  optionalQuery(query, "cursor", Stdlib_Option.map(cursor, value => value.toString()));
  return Stdlib_Result.map(await Http.get(client.http, "/api/trades", query, undefined, undefined, Trade__Raw.TradesResponse.t_decode), Trade__Raw.TradesResponse.toPage);
}

async function getByMarket(client, marketPubkey, limit, cursor) {
  let query = [[
      "market_pubkey",
      marketPubkey
    ]];
  optionalQuery(query, "limit", Stdlib_Option.map(limit, value => value.toString()));
  optionalQuery(query, "cursor", Stdlib_Option.map(cursor, value => value.toString()));
  return Stdlib_Result.map(await Http.get(client.http, "/api/trades/market", query, undefined, undefined, Trade__Raw.MarketTradesResponse.t_decode), Trade__Raw.MarketTradesResponse.toPage);
}

export {
  get,
  getByMarket,
}
/* Http Not a pure module */
