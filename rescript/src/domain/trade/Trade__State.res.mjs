// Generated by ReScript, PLEASE EDIT WITH CARE


function make(orderbookId, maxSize) {
  return {
    orderbookId: orderbookId,
    trades: [],
    maxSize: maxSize
  };
}

function push(state, trade) {
  if (state.maxSize <= 0) {
    return;
  }
  if (trade.sequence === 0.0) {
    let updated = [trade].concat(state.trades);
    state.trades = updated.length > state.maxSize ? updated.slice(0, state.maxSize) : updated;
    return;
  }
  let index = state.trades.findIndex(existing => existing.sequence < trade.sequence);
  let position = index === -1 ? state.trades.length : index;
  if (state.trades.length >= state.maxSize && position === state.trades.length) {
    return;
  }
  let updated$1 = state.trades.slice(0, position).concat([trade], state.trades.slice(position, state.trades.length));
  state.trades = updated$1.length > state.maxSize ? updated$1.slice(0, state.maxSize) : updated$1;
}

function replace(state, trades) {
  state.trades = trades.slice(0, state.maxSize);
}

function trades(state) {
  return state.trades;
}

function latest(state) {
  return state.trades[0];
}

function clear(state) {
  state.trades = [];
}

function size(state) {
  return state.trades.length;
}

function isEmpty(state) {
  return state.trades.length === 0;
}

export {
  make,
  push,
  replace,
  trades,
  latest,
  clear,
  size,
  isEmpty,
}
/* No side effect */
