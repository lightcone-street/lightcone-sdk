// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Http from "../Http.res.mjs";
import * as Spice from "@mununki/ppx-spice/src/rescript/Spice.res.mjs";
import * as Shared from "../Shared.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Stdlib_Result from "@rescript/runtime/lib/es6/Stdlib_Result.js";

function outcomeBalance_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let outcomeIndex = Stdlib_Option.getOr(Stdlib_Option.map(v["outcome_index"], Spice.floatFromJson), Spice.error(undefined, "outcome_index" + " missing", v));
  if (outcomeIndex.TAG === "Ok") {
    let conditionalToken = Stdlib_Option.getOr(Stdlib_Option.map(v["conditional_token"], Shared.pubkeyStr_decode), Spice.error(undefined, "conditional_token" + " missing", v));
    if (conditionalToken.TAG === "Ok") {
      let balance = Stdlib_Option.getOr(Stdlib_Option.map(v["balance"], Spice.stringFromJson), Spice.error(undefined, "balance" + " missing", v));
      if (balance.TAG === "Ok") {
        let balanceIdle = Stdlib_Option.getOr(Stdlib_Option.map(v["balance_idle"], Spice.stringFromJson), Spice.error(undefined, "balance_idle" + " missing", v));
        if (balanceIdle.TAG === "Ok") {
          let balanceOnBook = Stdlib_Option.getOr(Stdlib_Option.map(v["balance_on_book"], Spice.stringFromJson), Spice.error(undefined, "balance_on_book" + " missing", v));
          if (balanceOnBook.TAG === "Ok") {
            return {
              TAG: "Ok",
              _0: {
                outcomeIndex: outcomeIndex._0,
                conditionalToken: conditionalToken._0,
                balance: balance._0,
                balanceIdle: balanceIdle._0,
                balanceOnBook: balanceOnBook._0
              }
            };
          }
          let e = balanceOnBook._0;
          return Spice.error("." + ("balance_on_book" + e.path), e.message, e.value);
        }
        let e$1 = balanceIdle._0;
        return Spice.error("." + ("balance_idle" + e$1.path), e$1.message, e$1.value);
      }
      let e$2 = balance._0;
      return Spice.error("." + ("balance" + e$2.path), e$2.message, e$2.value);
    }
    let e$3 = conditionalToken._0;
    return Spice.error("." + ("conditional_token" + e$3.path), e$3.message, e$3.value);
  }
  let e$4 = outcomeIndex._0;
  return Spice.error("." + ("outcome_index" + e$4.path), e$4.message, e$4.value);
}

function vaultBalance_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let depositMint = Stdlib_Option.getOr(Stdlib_Option.map(v["deposit_mint"], Shared.pubkeyStr_decode), Spice.error(undefined, "deposit_mint" + " missing", v));
  if (depositMint.TAG === "Ok") {
    let vault = Stdlib_Option.getOr(Stdlib_Option.map(v["vault"], Shared.pubkeyStr_decode), Spice.error(undefined, "vault" + " missing", v));
    if (vault.TAG === "Ok") {
      let balance = Stdlib_Option.getOr(Stdlib_Option.map(v["balance"], Spice.stringFromJson), Spice.error(undefined, "balance" + " missing", v));
      if (balance.TAG === "Ok") {
        return {
          TAG: "Ok",
          _0: {
            depositMint: depositMint._0,
            vault: vault._0,
            balance: balance._0
          }
        };
      }
      let e = balance._0;
      return Spice.error("." + ("balance" + e.path), e.message, e.value);
    }
    let e$1 = vault._0;
    return Spice.error("." + ("vault" + e$1.path), e$1.message, e$1.value);
  }
  let e$2 = depositMint._0;
  return Spice.error("." + ("deposit_mint" + e$2.path), e$2.message, e$2.value);
}

function globalDeposit_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let depositMint = Stdlib_Option.getOr(Stdlib_Option.map(v["deposit_mint"], Shared.pubkeyStr_decode), Spice.error(undefined, "deposit_mint" + " missing", v));
  if (depositMint.TAG === "Ok") {
    let symbol = Stdlib_Option.getOr(Stdlib_Option.map(v["symbol"], Spice.stringFromJson), Spice.error(undefined, "symbol" + " missing", v));
    if (symbol.TAG === "Ok") {
      let balance = Stdlib_Option.getOr(Stdlib_Option.map(v["balance"], Spice.stringFromJson), Spice.error(undefined, "balance" + " missing", v));
      if (balance.TAG === "Ok") {
        return {
          TAG: "Ok",
          _0: {
            depositMint: depositMint._0,
            symbol: symbol._0,
            balance: balance._0
          }
        };
      }
      let e = balance._0;
      return Spice.error("." + ("balance" + e.path), e.message, e.value);
    }
    let e$1 = symbol._0;
    return Spice.error("." + ("symbol" + e$1.path), e$1.message, e$1.value);
  }
  let e$2 = depositMint._0;
  return Spice.error("." + ("deposit_mint" + e$2.path), e$2.message, e$2.value);
}

function positionEntry_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let id = Stdlib_Option.getOr(Stdlib_Option.map(v["id"], Spice.floatFromJson), Spice.error(undefined, "id" + " missing", v));
  if (id.TAG === "Ok") {
    let positionPubkey = Stdlib_Option.getOr(Stdlib_Option.map(v["position_pubkey"], Shared.pubkeyStr_decode), Spice.error(undefined, "position_pubkey" + " missing", v));
    if (positionPubkey.TAG === "Ok") {
      let owner = Stdlib_Option.getOr(Stdlib_Option.map(v["owner"], Shared.pubkeyStr_decode), Spice.error(undefined, "owner" + " missing", v));
      if (owner.TAG === "Ok") {
        let marketPubkey = Stdlib_Option.getOr(Stdlib_Option.map(v["market_pubkey"], Shared.pubkeyStr_decode), Spice.error(undefined, "market_pubkey" + " missing", v));
        if (marketPubkey.TAG === "Ok") {
          let outcomes = Stdlib_Option.getOr(Stdlib_Option.map(v["outcomes"], extra => Spice.arrayFromJson(outcomeBalance_decode, extra)), Spice.error(undefined, "outcomes" + " missing", v));
          if (outcomes.TAG === "Ok") {
            let vaultBalances = Stdlib_Option.getOr(Stdlib_Option.map(v["vault_balances"], extra => Spice.arrayFromJson(vaultBalance_decode, extra)), {
              TAG: "Ok",
              _0: []
            });
            if (vaultBalances.TAG === "Ok") {
              let createdAt = Stdlib_Option.getOr(Stdlib_Option.map(v["created_at"], Spice.stringFromJson), Spice.error(undefined, "created_at" + " missing", v));
              if (createdAt.TAG === "Ok") {
                let updatedAt = Stdlib_Option.getOr(Stdlib_Option.map(v["updated_at"], Spice.stringFromJson), Spice.error(undefined, "updated_at" + " missing", v));
                if (updatedAt.TAG === "Ok") {
                  return {
                    TAG: "Ok",
                    _0: {
                      id: id._0,
                      positionPubkey: positionPubkey._0,
                      owner: owner._0,
                      marketPubkey: marketPubkey._0,
                      outcomes: outcomes._0,
                      vaultBalances: vaultBalances._0,
                      createdAt: createdAt._0,
                      updatedAt: updatedAt._0
                    }
                  };
                }
                let e = updatedAt._0;
                return Spice.error("." + ("updated_at" + e.path), e.message, e.value);
              }
              let e$1 = createdAt._0;
              return Spice.error("." + ("created_at" + e$1.path), e$1.message, e$1.value);
            }
            let e$2 = vaultBalances._0;
            return Spice.error("." + ("vault_balances" + e$2.path), e$2.message, e$2.value);
          }
          let e$3 = outcomes._0;
          return Spice.error("." + ("outcomes" + e$3.path), e$3.message, e$3.value);
        }
        let e$4 = marketPubkey._0;
        return Spice.error("." + ("market_pubkey" + e$4.path), e$4.message, e$4.value);
      }
      let e$5 = owner._0;
      return Spice.error("." + ("owner" + e$5.path), e$5.message, e$5.value);
    }
    let e$6 = positionPubkey._0;
    return Spice.error("." + ("position_pubkey" + e$6.path), e$6.message, e$6.value);
  }
  let e$7 = id._0;
  return Spice.error("." + ("id" + e$7.path), e$7.message, e$7.value);
}

function positionsResponse_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let owner = Stdlib_Option.getOr(Stdlib_Option.map(v["owner"], Shared.pubkeyStr_decode), Spice.error(undefined, "owner" + " missing", v));
  if (owner.TAG === "Ok") {
    let totalMarkets = Stdlib_Option.getOr(Stdlib_Option.map(v["total_markets"], Spice.floatFromJson), Spice.error(undefined, "total_markets" + " missing", v));
    if (totalMarkets.TAG === "Ok") {
      let positions = Stdlib_Option.getOr(Stdlib_Option.map(v["positions"], extra => Spice.arrayFromJson(positionEntry_decode, extra)), Spice.error(undefined, "positions" + " missing", v));
      if (positions.TAG === "Ok") {
        let globalDeposits = Stdlib_Option.getOr(Stdlib_Option.map(v["global_deposits"], extra => Spice.arrayFromJson(globalDeposit_decode, extra)), {
          TAG: "Ok",
          _0: []
        });
        if (globalDeposits.TAG === "Ok") {
          let decimals = Stdlib_Option.getOr(Stdlib_Option.map(v["decimals"], extra => Spice.dictFromJson(Spice.floatFromJson, extra)), Spice.error(undefined, "decimals" + " missing", v));
          if (decimals.TAG === "Ok") {
            return {
              TAG: "Ok",
              _0: {
                owner: owner._0,
                totalMarkets: totalMarkets._0,
                positions: positions._0,
                globalDeposits: globalDeposits._0,
                decimals: decimals._0
              }
            };
          }
          let e = decimals._0;
          return Spice.error("." + ("decimals" + e.path), e.message, e.value);
        }
        let e$1 = globalDeposits._0;
        return Spice.error("." + ("global_deposits" + e$1.path), e$1.message, e$1.value);
      }
      let e$2 = positions._0;
      return Spice.error("." + ("positions" + e$2.path), e$2.message, e$2.value);
    }
    let e$3 = totalMarkets._0;
    return Spice.error("." + ("total_markets" + e$3.path), e$3.message, e$3.value);
  }
  let e$4 = owner._0;
  return Spice.error("." + ("owner" + e$4.path), e$4.message, e$4.value);
}

function marketPositionsResponse_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let owner = Stdlib_Option.getOr(Stdlib_Option.map(v["owner"], Shared.pubkeyStr_decode), Spice.error(undefined, "owner" + " missing", v));
  if (owner.TAG === "Ok") {
    let marketPubkey = Stdlib_Option.getOr(Stdlib_Option.map(v["market_pubkey"], Shared.pubkeyStr_decode), Spice.error(undefined, "market_pubkey" + " missing", v));
    if (marketPubkey.TAG === "Ok") {
      let positions = Stdlib_Option.getOr(Stdlib_Option.map(v["positions"], extra => Spice.arrayFromJson(positionEntry_decode, extra)), Spice.error(undefined, "positions" + " missing", v));
      if (positions.TAG === "Ok") {
        let globalDeposits = Stdlib_Option.getOr(Stdlib_Option.map(v["global_deposits"], extra => Spice.arrayFromJson(globalDeposit_decode, extra)), {
          TAG: "Ok",
          _0: []
        });
        if (globalDeposits.TAG === "Ok") {
          let decimals = Stdlib_Option.getOr(Stdlib_Option.map(v["decimals"], extra => Spice.dictFromJson(Spice.floatFromJson, extra)), Spice.error(undefined, "decimals" + " missing", v));
          if (decimals.TAG === "Ok") {
            return {
              TAG: "Ok",
              _0: {
                owner: owner._0,
                marketPubkey: marketPubkey._0,
                positions: positions._0,
                globalDeposits: globalDeposits._0,
                decimals: decimals._0
              }
            };
          }
          let e = decimals._0;
          return Spice.error("." + ("decimals" + e.path), e.message, e.value);
        }
        let e$1 = globalDeposits._0;
        return Spice.error("." + ("global_deposits" + e$1.path), e$1.message, e$1.value);
      }
      let e$2 = positions._0;
      return Spice.error("." + ("positions" + e$2.path), e$2.message, e$2.value);
    }
    let e$3 = marketPubkey._0;
    return Spice.error("." + ("market_pubkey" + e$3.path), e$3.message, e$3.value);
  }
  let e$4 = owner._0;
  return Spice.error("." + ("owner" + e$4.path), e$4.message, e$4.value);
}

function depositTokenBalance_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let mint = Stdlib_Option.getOr(Stdlib_Option.map(v["mint"], Shared.pubkeyStr_decode), Spice.error(undefined, "mint" + " missing", v));
  if (mint.TAG === "Ok") {
    let idle = Stdlib_Option.getOr(Stdlib_Option.map(v["idle"], Spice.stringFromJson), Spice.error(undefined, "idle" + " missing", v));
    if (idle.TAG === "Ok") {
      let symbol = Stdlib_Option.getOr(Stdlib_Option.map(v["symbol"], Spice.stringFromJson), Spice.error(undefined, "symbol" + " missing", v));
      if (symbol.TAG === "Ok") {
        let name = Stdlib_Option.getOr(Stdlib_Option.map(v["name"], Spice.stringFromJson), Spice.error(undefined, "name" + " missing", v));
        if (name.TAG === "Ok") {
          let iconUrlLow = Stdlib_Option.getOr(Stdlib_Option.map(v["icon_url_low"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
            TAG: "Ok",
            _0: undefined
          });
          if (iconUrlLow.TAG === "Ok") {
            let iconUrlMedium = Stdlib_Option.getOr(Stdlib_Option.map(v["icon_url_medium"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
              TAG: "Ok",
              _0: undefined
            });
            if (iconUrlMedium.TAG === "Ok") {
              let iconUrlHigh = Stdlib_Option.getOr(Stdlib_Option.map(v["icon_url_high"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
                TAG: "Ok",
                _0: undefined
              });
              if (iconUrlHigh.TAG === "Ok") {
                return {
                  TAG: "Ok",
                  _0: {
                    mint: mint._0,
                    idle: idle._0,
                    symbol: symbol._0,
                    name: name._0,
                    iconUrlLow: iconUrlLow._0,
                    iconUrlMedium: iconUrlMedium._0,
                    iconUrlHigh: iconUrlHigh._0
                  }
                };
              }
              let e = iconUrlHigh._0;
              return Spice.error("." + ("icon_url_high" + e.path), e.message, e.value);
            }
            let e$1 = iconUrlMedium._0;
            return Spice.error("." + ("icon_url_medium" + e$1.path), e$1.message, e$1.value);
          }
          let e$2 = iconUrlLow._0;
          return Spice.error("." + ("icon_url_low" + e$2.path), e$2.message, e$2.value);
        }
        let e$3 = name._0;
        return Spice.error("." + ("name" + e$3.path), e$3.message, e$3.value);
      }
      let e$4 = symbol._0;
      return Spice.error("." + ("symbol" + e$4.path), e$4.message, e$4.value);
    }
    let e$5 = idle._0;
    return Spice.error("." + ("idle" + e$5.path), e$5.message, e$5.value);
  }
  let e$6 = mint._0;
  return Spice.error("." + ("mint" + e$6.path), e$6.message, e$6.value);
}

async function get(client, userPubkey) {
  return await Http.get(client.http, `/api/users/` + userPubkey + `/positions`, undefined, undefined, undefined, positionsResponse_decode);
}

async function getForMarket(client, userPubkey, marketPubkey) {
  return await Http.get(client.http, `/api/users/` + userPubkey + `/markets/` + marketPubkey + `/positions`, undefined, undefined, undefined, marketPositionsResponse_decode);
}

async function positions(client, cookieHeader) {
  return await Http.get(client.http, "/api/users/positions", undefined, undefined, cookieHeader, positionsResponse_decode);
}

async function positionsForMarket(client, marketPubkey, cookieHeader) {
  return await Http.get(client.http, `/api/users/markets/` + marketPubkey + `/positions`, undefined, undefined, cookieHeader, marketPositionsResponse_decode);
}

async function depositTokenBalances(client, cookieHeader) {
  return await Http.get(client.http, "/api/users/deposit-token-balances", undefined, undefined, cookieHeader, json => Spice.dictFromJson(depositTokenBalance_decode, json));
}

export {
  get,
  getForMarket,
  positions,
  positionsForMarket,
  depositTokenBalances,
}
/* Http Not a pure module */
