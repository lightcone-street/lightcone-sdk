// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Http from "../../Http.res.mjs";
import * as Spice from "@mununki/ppx-spice/src/rescript/Spice.res.mjs";
import * as Position__Raw from "./Position__Raw.res.mjs";

async function get(client, userPubkey) {
  return await Http.get(client.http, `/api/users/` + userPubkey + `/positions`, undefined, undefined, undefined, Position__Raw.PositionsResponse.t_decode);
}

async function getForMarket(client, userPubkey, marketPubkey) {
  return await Http.get(client.http, `/api/users/` + userPubkey + `/markets/` + marketPubkey + `/positions`, undefined, undefined, undefined, Position__Raw.MarketPositionsResponse.t_decode);
}

async function positions(client, cookieHeader) {
  return await Http.get(client.http, "/api/users/positions", undefined, undefined, cookieHeader, Position__Raw.PositionsResponse.t_decode);
}

async function positionsForMarket(client, marketPubkey, cookieHeader) {
  return await Http.get(client.http, `/api/users/markets/` + marketPubkey + `/positions`, undefined, undefined, cookieHeader, Position__Raw.MarketPositionsResponse.t_decode);
}

async function depositTokenBalances(client, cookieHeader) {
  return await Http.get(client.http, "/api/users/deposit-token-balances", undefined, undefined, cookieHeader, json => Spice.dictFromJson(Position__Raw.DepositTokenBalance.t_decode, json));
}

export {
  get,
  getForMarket,
  positions,
  positionsForMarket,
  depositTokenBalances,
}
/* Http Not a pure module */
