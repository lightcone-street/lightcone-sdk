// Generated by ReScript, PLEASE EDIT WITH CARE

import * as Order from "../domain/Order.res.mjs";
import * as Spice from "@mununki/ppx-spice/src/rescript/Spice.res.mjs";
import * as Shared from "../Shared.res.mjs";
import * as SdkError from "../SdkError.res.mjs";
import * as Orderbook from "../domain/Orderbook.res.mjs";
import * as Notification from "../domain/Notification.res.mjs";
import * as PriceHistory from "../domain/PriceHistory.res.mjs";
import * as Stdlib_Option from "@rescript/runtime/lib/es6/Stdlib_Option.js";
import * as Stdlib_Result from "@rescript/runtime/lib/es6/Stdlib_Result.js";

function optString(dict, key) {
  let match = dict[key];
  if (typeof match === "string") {
    return match;
  }
}

function optFloat(dict, key) {
  let match = dict[key];
  if (typeof match === "number") {
    return match;
  }
}

function wsErrorFrame_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let error = Stdlib_Option.getOr(Stdlib_Option.map(v["error"], Spice.stringFromJson), Spice.error(undefined, "error" + " missing", v));
  if (error.TAG === "Ok") {
    let code = Stdlib_Option.getOr(Stdlib_Option.map(v["code"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
      TAG: "Ok",
      _0: undefined
    });
    if (code.TAG === "Ok") {
      let orderbookId = Stdlib_Option.getOr(Stdlib_Option.map(v["orderbook_id"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
        TAG: "Ok",
        _0: undefined
      });
      if (orderbookId.TAG === "Ok") {
        let nSigFigs = Stdlib_Option.getOr(Stdlib_Option.map(v["n_sig_figs"], json => Stdlib_Result.map(Spice.intFromJson(json), v => v)), {
          TAG: "Ok",
          _0: undefined
        });
        if (nSigFigs.TAG === "Ok") {
          let mantissa = Stdlib_Option.getOr(Stdlib_Option.map(v["mantissa"], json => Stdlib_Result.map(Spice.intFromJson(json), v => v)), {
            TAG: "Ok",
            _0: undefined
          });
          if (mantissa.TAG === "Ok") {
            let walletAddress = Stdlib_Option.getOr(Stdlib_Option.map(v["wallet_address"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
              TAG: "Ok",
              _0: undefined
            });
            if (walletAddress.TAG === "Ok") {
              let depositAsset = Stdlib_Option.getOr(Stdlib_Option.map(v["deposit_asset"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
                TAG: "Ok",
                _0: undefined
              });
              if (depositAsset.TAG === "Ok") {
                let hint = Stdlib_Option.getOr(Stdlib_Option.map(v["hint"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
                  TAG: "Ok",
                  _0: undefined
                });
                if (hint.TAG === "Ok") {
                  let details = Stdlib_Option.getOr(Stdlib_Option.map(v["details"], json => Stdlib_Result.map(Spice.stringFromJson(json), v => v)), {
                    TAG: "Ok",
                    _0: undefined
                  });
                  if (details.TAG === "Ok") {
                    return {
                      TAG: "Ok",
                      _0: {
                        error: error._0,
                        code: code._0,
                        orderbookId: orderbookId._0,
                        nSigFigs: nSigFigs._0,
                        mantissa: mantissa._0,
                        walletAddress: walletAddress._0,
                        depositAsset: depositAsset._0,
                        hint: hint._0,
                        details: details._0
                      }
                    };
                  }
                  let e = details._0;
                  return Spice.error("." + ("details" + e.path), e.message, e.value);
                }
                let e$1 = hint._0;
                return Spice.error("." + ("hint" + e$1.path), e$1.message, e$1.value);
              }
              let e$2 = depositAsset._0;
              return Spice.error("." + ("deposit_asset" + e$2.path), e$2.message, e$2.value);
            }
            let e$3 = walletAddress._0;
            return Spice.error("." + ("wallet_address" + e$3.path), e$3.message, e$3.value);
          }
          let e$4 = mantissa._0;
          return Spice.error("." + ("mantissa" + e$4.path), e$4.message, e$4.value);
        }
        let e$5 = nSigFigs._0;
        return Spice.error("." + ("n_sig_figs" + e$5.path), e$5.message, e$5.value);
      }
      let e$6 = orderbookId._0;
      return Spice.error("." + ("orderbook_id" + e$6.path), e$6.message, e$6.value);
    }
    let e$7 = code._0;
    return Spice.error("." + ("code" + e$7.path), e$7.message, e$7.value);
  }
  let e$8 = error._0;
  return Spice.error("." + ("error" + e$8.path), e$8.message, e$8.value);
}

function wsTrade_decode(v) {
  if (typeof v !== "object" || v === null || Array.isArray(v)) {
    return Spice.error(undefined, "Not an object", v);
  }
  let orderbookId = Stdlib_Option.getOr(Stdlib_Option.map(v["orderbook_id"], Shared.orderBookId_decode), Spice.error(undefined, "orderbook_id" + " missing", v));
  if (orderbookId.TAG === "Ok") {
    let tradeId = Stdlib_Option.getOr(Stdlib_Option.map(v["trade_id"], Spice.stringFromJson), Spice.error(undefined, "trade_id" + " missing", v));
    if (tradeId.TAG === "Ok") {
      let timestamp = Stdlib_Option.getOr(Stdlib_Option.map(v["timestamp"], Spice.stringFromJson), Spice.error(undefined, "timestamp" + " missing", v));
      if (timestamp.TAG === "Ok") {
        let price = Stdlib_Option.getOr(Stdlib_Option.map(v["price"], Spice.stringFromJson), Spice.error(undefined, "price" + " missing", v));
        if (price.TAG === "Ok") {
          let size = Stdlib_Option.getOr(Stdlib_Option.map(v["size"], Spice.stringFromJson), Spice.error(undefined, "size" + " missing", v));
          if (size.TAG === "Ok") {
            let side = Stdlib_Option.getOr(Stdlib_Option.map(v["side"], Shared.Side.t_decode), Spice.error(undefined, "side" + " missing", v));
            if (side.TAG === "Ok") {
              let sequence = Stdlib_Option.getOr(Stdlib_Option.map(v["sequence"], Spice.floatFromJson), {
                TAG: "Ok",
                _0: 0.0
              });
              if (sequence.TAG === "Ok") {
                return {
                  TAG: "Ok",
                  _0: {
                    orderbookId: orderbookId._0,
                    tradeId: tradeId._0,
                    timestamp: timestamp._0,
                    price: price._0,
                    size: size._0,
                    side: side._0,
                    sequence: sequence._0
                  }
                };
              }
              let e = sequence._0;
              return Spice.error("." + ("sequence" + e.path), e.message, e.value);
            }
            let e$1 = side._0;
            return Spice.error("." + ("side" + e$1.path), e$1.message, e$1.value);
          }
          let e$2 = size._0;
          return Spice.error("." + ("size" + e$2.path), e$2.message, e$2.value);
        }
        let e$3 = price._0;
        return Spice.error("." + ("price" + e$3.path), e$3.message, e$3.value);
      }
      let e$4 = timestamp._0;
      return Spice.error("." + ("timestamp" + e$4.path), e$4.message, e$4.value);
    }
    let e$5 = tradeId._0;
    return Spice.error("." + ("trade_id" + e$5.path), e$5.message, e$5.value);
  }
  let e$6 = orderbookId._0;
  return Spice.error("." + ("orderbook_id" + e$6.path), e$6.message, e$6.value);
}

function wsTickerDecode(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "ticker payload is not an object", json);
  }
  let match = json["orderbook_id"];
  if (match === undefined) {
    return Spice.error(undefined, "ticker payload missing orderbook_id", json);
  }
  if (typeof match !== "string") {
    return Spice.error(undefined, "ticker payload missing orderbook_id", json);
  }
  let value = optString(json, "mid");
  let mid = value !== undefined ? value : optString(json, "mid_price");
  return {
    TAG: "Ok",
    _0: {
      orderbookId: match,
      bestBid: optString(json, "best_bid"),
      bestAsk: optString(json, "best_ask"),
      mid: mid
    }
  };
}

function decode(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "market event is not an object", json);
  }
  let match = json["event_type"];
  let match$1 = optString(json, "market_pubkey");
  if (match === undefined) {
    return Spice.error(undefined, "market event missing event_type/market_pubkey", json);
  }
  if (typeof match !== "string") {
    return Spice.error(undefined, "market event missing event_type/market_pubkey", json);
  }
  switch (match) {
    case "created" :
      if (match$1 !== undefined) {
        return {
          TAG: "Ok",
          _0: {
            TAG: "Created",
            _0: match$1
          }
        };
      }
      break;
    case "opened" :
      if (match$1 !== undefined) {
        return {
          TAG: "Ok",
          _0: {
            TAG: "Opened",
            _0: match$1
          }
        };
      }
      break;
    case "orderbook_created" :
      if (match$1 !== undefined) {
        let orderbookId = optString(json, "orderbook_id");
        if (orderbookId !== undefined) {
          return {
            TAG: "Ok",
            _0: {
              TAG: "OrderbookCreated",
              _0: match$1,
              _1: orderbookId
            }
          };
        } else {
          return Spice.error(undefined, "orderbook_created market event missing orderbook_id", json);
        }
      }
      break;
    case "paused" :
      if (match$1 !== undefined) {
        return {
          TAG: "Ok",
          _0: {
            TAG: "Paused",
            _0: match$1
          }
        };
      }
      break;
    case "settled" :
      if (match$1 !== undefined) {
        return {
          TAG: "Ok",
          _0: {
            TAG: "Settled",
            _0: match$1
          }
        };
      }
      break;
  }
  return Spice.error(undefined, `unknown market event_type: ` + match, json);
}

function decode$1(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "auth update is not an object", json);
  }
  let match = json["status"];
  if (match === undefined) {
    return Spice.error(undefined, "unknown auth status", json);
  }
  if (typeof match !== "string") {
    return Spice.error(undefined, "unknown auth status", json);
  }
  switch (match) {
    case "anonymous" :
      return {
        TAG: "Ok",
        _0: {
          TAG: "Anonymous",
          _0: optString(json, "reason")
        }
      };
    case "authenticated" :
      let wallet = optString(json, "wallet");
      if (wallet !== undefined) {
        return {
          TAG: "Ok",
          _0: {
            TAG: "Authenticated",
            _0: wallet
          }
        };
      } else {
        return Spice.error(undefined, "authenticated auth update missing wallet", json);
      }
    default:
      return Spice.error(undefined, "unknown auth status", json);
  }
}

function decodeSnapshot(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "price_history snapshot is not an object", json);
  }
  let match = json["orderbook_id"];
  let match$1 = json["resolution"];
  let match$2 = json["prices"];
  if (match === undefined) {
    return Spice.error(undefined, "price_history snapshot missing fields", json);
  }
  if (typeof match !== "string") {
    return Spice.error(undefined, "price_history snapshot missing fields", json);
  }
  if (match$1 === undefined) {
    return Spice.error(undefined, "price_history snapshot missing fields", json);
  }
  if (match$2 === undefined) {
    return Spice.error(undefined, "price_history snapshot missing fields", json);
  }
  let match$3 = Shared.Resolution.t_decode(match$1);
  let match$4 = Spice.arrayFromJson(PriceHistory.orderbookPriceCandle_decode, match$2);
  if (match$3.TAG === "Ok") {
    if (match$4.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: {
          orderbookId: match,
          resolution: match$3._0,
          prices: match$4._0,
          lastTimestamp: optFloat(json, "last_timestamp"),
          serverTime: optFloat(json, "server_time")
        }
      };
    } else {
      return {
        TAG: "Error",
        _0: match$4._0
      };
    }
  } else {
    return {
      TAG: "Error",
      _0: match$3._0
    };
  }
}

function decodeUpdate(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "price_history update is not an object", json);
  }
  let match = json["orderbook_id"];
  let match$1 = json["resolution"];
  if (match === undefined) {
    return Spice.error(undefined, "price_history update missing fields", json);
  }
  if (typeof match !== "string") {
    return Spice.error(undefined, "price_history update missing fields", json);
  }
  if (match$1 === undefined) {
    return Spice.error(undefined, "price_history update missing fields", json);
  }
  let match$2 = Shared.Resolution.t_decode(match$1);
  let match$3 = PriceHistory.orderbookPriceCandle_decode(json);
  if (match$2.TAG === "Ok") {
    if (match$3.TAG === "Ok") {
      return {
        TAG: "Ok",
        _0: {
          orderbookId: match,
          resolution: match$2._0,
          candle: match$3._0
        }
      };
    } else {
      return {
        TAG: "Error",
        _0: match$3._0
      };
    }
  } else {
    return {
      TAG: "Error",
      _0: match$2._0
    };
  }
}

function decodeHeartbeat(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "price_history heartbeat is not an object", json);
  }
  let serverTime = optFloat(json, "server_time");
  if (serverTime !== undefined) {
    return {
      TAG: "Ok",
      _0: {
        serverTime: serverTime,
        lastProcessed: optFloat(json, "last_processed")
      }
    };
  } else {
    return Spice.error(undefined, "price_history heartbeat missing server_time", json);
  }
}

function decode$2(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "price_history payload is not an object", json);
  }
  let match = json["event_type"];
  if (match === undefined) {
    return Spice.error(undefined, "unknown price_history event_type", json);
  }
  if (typeof match !== "string") {
    return Spice.error(undefined, "unknown price_history event_type", json);
  }
  switch (match) {
    case "heartbeat" :
      return Stdlib_Result.map(decodeHeartbeat(json), value => ({
        TAG: "Heartbeat",
        _0: value
      }));
    case "snapshot" :
      return Stdlib_Result.map(decodeSnapshot(json), value => ({
        TAG: "Snapshot",
        _0: value
      }));
    case "update" :
      return Stdlib_Result.map(decodeUpdate(json), value => ({
        TAG: "Update",
        _0: value
      }));
    default:
      return Spice.error(undefined, "unknown price_history event_type", json);
  }
}

function decode$3(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "deposit_price payload is not an object", json);
  }
  let match = json["event_type"];
  if (match === undefined) {
    return Spice.error(undefined, "unknown deposit_price event_type", json);
  }
  if (typeof match !== "string") {
    return Spice.error(undefined, "unknown deposit_price event_type", json);
  }
  switch (match) {
    case "candle" :
      let match$1 = json["deposit_asset"];
      let match$2 = json["resolution"];
      let match$3 = json["t"];
      let match$4 = json["tc"];
      let match$5 = json["c"];
      if (match$1 === undefined) {
        return Spice.error(undefined, "deposit_price candle missing fields", json);
      }
      if (typeof match$1 !== "string") {
        return Spice.error(undefined, "deposit_price candle missing fields", json);
      }
      if (match$2 === undefined) {
        return Spice.error(undefined, "deposit_price candle missing fields", json);
      }
      if (match$3 === undefined) {
        return Spice.error(undefined, "deposit_price candle missing fields", json);
      }
      if (typeof match$3 !== "number") {
        return Spice.error(undefined, "deposit_price candle missing fields", json);
      }
      if (match$4 === undefined) {
        return Spice.error(undefined, "deposit_price candle missing fields", json);
      }
      if (typeof match$4 !== "number") {
        return Spice.error(undefined, "deposit_price candle missing fields", json);
      }
      if (match$5 === undefined) {
        return Spice.error(undefined, "deposit_price candle missing fields", json);
      }
      if (typeof match$5 !== "string") {
        return Spice.error(undefined, "deposit_price candle missing fields", json);
      }
      let resolution = Shared.Resolution.t_decode(match$2);
      if (resolution.TAG === "Ok") {
        return {
          TAG: "Ok",
          _0: {
            TAG: "Candle",
            _0: {
              depositAsset: match$1,
              resolution: resolution._0,
              t: match$3,
              tc: match$4,
              c: match$5
            }
          }
        };
      } else {
        return {
          TAG: "Error",
          _0: resolution._0
        };
      }
    case "price" :
      let match$6 = json["deposit_asset"];
      let match$7 = json["price"];
      let match$8 = json["event_time"];
      if (typeof match$6 === "string" && typeof match$7 === "string" && typeof match$8 === "number") {
        return {
          TAG: "Ok",
          _0: {
            TAG: "Price",
            _0: {
              depositAsset: match$6,
              price: match$7,
              eventTime: match$8
            }
          }
        };
      } else {
        return Spice.error(undefined, "deposit_price tick missing fields", json);
      }
    case "snapshot" :
      let match$9 = json["deposit_asset"];
      let match$10 = json["resolution"];
      let match$11 = json["prices"];
      if (match$9 === undefined) {
        return Spice.error(undefined, "deposit_price snapshot missing fields", json);
      }
      if (typeof match$9 !== "string") {
        return Spice.error(undefined, "deposit_price snapshot missing fields", json);
      }
      if (match$10 === undefined) {
        return Spice.error(undefined, "deposit_price snapshot missing fields", json);
      }
      if (match$11 === undefined) {
        return Spice.error(undefined, "deposit_price snapshot missing fields", json);
      }
      let match$12 = Shared.Resolution.t_decode(match$10);
      let match$13 = Spice.arrayFromJson(PriceHistory.depositPriceCandle_decode, match$11);
      if (match$12.TAG === "Ok") {
        if (match$13.TAG === "Ok") {
          return {
            TAG: "Ok",
            _0: {
              TAG: "Snapshot",
              _0: {
                depositAsset: match$9,
                resolution: match$12._0,
                prices: match$13._0
              }
            }
          };
        } else {
          return {
            TAG: "Error",
            _0: match$13._0
          };
        }
      } else {
        return {
          TAG: "Error",
          _0: match$12._0
        };
      }
    default:
      return Spice.error(undefined, "unknown deposit_price event_type", json);
  }
}

function decode$4(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "deposit_asset_price payload is not an object", json);
  }
  let match = json["event_type"];
  if (match === undefined) {
    return Spice.error(undefined, "unknown deposit_asset_price event_type", json);
  }
  if (typeof match !== "string") {
    return Spice.error(undefined, "unknown deposit_asset_price event_type", json);
  }
  switch (match) {
    case "price" :
      let match$1 = json["deposit_asset"];
      let match$2 = json["price"];
      let match$3 = json["event_time"];
      if (typeof match$1 === "string" && typeof match$2 === "string" && typeof match$3 === "number") {
        return {
          TAG: "Ok",
          _0: {
            TAG: "Price",
            _0: {
              depositAsset: match$1,
              price: match$2,
              eventTime: match$3
            }
          }
        };
      } else {
        return Spice.error(undefined, "deposit_asset_price tick missing fields", json);
      }
    case "snapshot" :
      let match$4 = json["deposit_asset"];
      let match$5 = json["price"];
      if (typeof match$4 === "string" && typeof match$5 === "string") {
        return {
          TAG: "Ok",
          _0: {
            TAG: "Snapshot",
            _0: {
              depositAsset: match$4,
              price: match$5
            }
          }
        };
      } else {
        return Spice.error(undefined, "deposit_asset_price snapshot missing fields", json);
      }
    default:
      return Spice.error(undefined, "unknown deposit_asset_price event_type", json);
  }
}

function userBalanceUpdateDecode(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "user balance update is not an object", json);
  }
  let match = json["market_pubkey"];
  let match$1 = json["market_balance"];
  let match$2 = json["timestamp"];
  if (match === undefined) {
    return Spice.error(undefined, "user balance update missing fields", json);
  }
  if (typeof match !== "string") {
    return Spice.error(undefined, "user balance update missing fields", json);
  }
  if (match$1 === undefined) {
    return Spice.error(undefined, "user balance update missing fields", json);
  }
  if (match$2 === undefined) {
    return Spice.error(undefined, "user balance update missing fields", json);
  }
  if (typeof match$2 !== "string") {
    return Spice.error(undefined, "user balance update missing fields", json);
  }
  let marketBalance = Order.userMarketBalance_decode(match$1);
  if (marketBalance.TAG === "Ok") {
    return {
      TAG: "Ok",
      _0: {
        marketPubkey: match,
        marketBalance: marketBalance._0,
        timestamp: match$2
      }
    };
  } else {
    return {
      TAG: "Error",
      _0: marketBalance._0
    };
  }
}

function globalDepositUpdateDecode(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "global deposit update is not an object", json);
  }
  let match = json["mint"];
  let match$1 = json["balance"];
  let match$2 = json["timestamp"];
  if (typeof match === "string" && typeof match$1 === "string" && typeof match$2 === "string") {
    return {
      TAG: "Ok",
      _0: {
        mint: match,
        balance: match$1,
        timestamp: match$2
      }
    };
  } else {
    return Spice.error(undefined, "global deposit update missing fields", json);
  }
}

function nonceUpdateDecode(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "nonce update is not an object", json);
  }
  let match = json["user_pubkey"];
  let match$1 = json["new_nonce"];
  let match$2 = json["timestamp"];
  if (typeof match === "string" && typeof match$1 === "number" && typeof match$2 === "string") {
    return {
      TAG: "Ok",
      _0: {
        userPubkey: match,
        newNonce: match$1,
        timestamp: match$2
      }
    };
  } else {
    return Spice.error(undefined, "nonce update missing fields", json);
  }
}

function decode$5(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "user payload is not an object", json);
  }
  let match = json["event_type"];
  if (match === undefined) {
    return Spice.error(undefined, "unknown user event_type", json);
  }
  if (typeof match !== "string") {
    return Spice.error(undefined, "unknown user event_type", json);
  }
  switch (match) {
    case "global_deposit_update" :
      return Stdlib_Result.map(globalDepositUpdateDecode(json), value => ({
        TAG: "GlobalDepositUpdate",
        _0: value
      }));
    case "market_balance_update" :
      return Stdlib_Result.map(userBalanceUpdateDecode(json), value => ({
        TAG: "BalanceUpdate",
        _0: value
      }));
    case "nonce" :
      return Stdlib_Result.map(nonceUpdateDecode(json), value => ({
        TAG: "NonceUpdate",
        _0: value
      }));
    case "notification" :
      let notificationJson = json["notification"];
      if (notificationJson !== undefined) {
        return Stdlib_Result.map(Notification.notificationDecode(notificationJson), value => ({
          TAG: "NotificationPush",
          _0: value
        }));
      } else {
        return Spice.error(undefined, "user notification event missing 'notification'", json);
      }
    case "order" :
      return Stdlib_Result.map(Order.OrderEvent.decode(json), value => ({
        TAG: "Order",
        _0: value
      }));
    case "snapshot" :
      return Stdlib_Result.map(Order.userSnapshotDecode(json), value => ({
        TAG: "Snapshot",
        _0: value
      }));
    default:
      return Spice.error(undefined, "unknown user event_type", json);
  }
}

function kindDecode(json) {
  if (typeof json !== "object" || json === null || Array.isArray(json)) {
    return Spice.error(undefined, "ws message is not an object", json);
  }
  let data = Stdlib_Option.getOr(json["data"], null);
  let match = json["type"];
  if (match === undefined) {
    return Spice.error(undefined, "ws message missing 'type'", json);
  }
  if (typeof match !== "string") {
    return Spice.error(undefined, "ws message missing 'type'", json);
  }
  switch (match) {
    case "auth" :
      return Stdlib_Result.map(decode$1(data), value => ({
        TAG: "Auth",
        _0: value
      }));
    case "book_update" :
      return Stdlib_Result.map(Orderbook.orderBook_decode(data), value => ({
        TAG: "BookUpdate",
        _0: value
      }));
    case "deposit_asset_price" :
      return Stdlib_Result.map(decode$4(data), value => ({
        TAG: "DepositAssetPrice",
        _0: value
      }));
    case "deposit_price" :
      return Stdlib_Result.map(decode$3(data), value => ({
        TAG: "DepositPrice",
        _0: value
      }));
    case "error" :
      return Stdlib_Result.map(wsErrorFrame_decode(data), value => ({
        TAG: "ErrorFrame",
        _0: value
      }));
    case "market" :
      return Stdlib_Result.map(decode(data), value => ({
        TAG: "Market",
        _0: value
      }));
    case "pong" :
      return {
        TAG: "Ok",
        _0: "Pong"
      };
    case "price_history" :
      return Stdlib_Result.map(decode$2(data), value => ({
        TAG: "PriceHistory",
        _0: value
      }));
    case "ticker" :
      return Stdlib_Result.map(wsTickerDecode(data), value => ({
        TAG: "Ticker",
        _0: value
      }));
    case "trades" :
      return Stdlib_Result.map(wsTrade_decode(data), value => ({
        TAG: "Trades",
        _0: value
      }));
    case "user" :
      return Stdlib_Result.map(decode$5(data), value => ({
        TAG: "User",
        _0: value
      }));
    default:
      return Spice.error(undefined, `unknown ws message type: ` + match, json);
  }
}

function decodeMessage(json) {
  let stripped = SdkError.stripNulls(json);
  let kind = kindDecode(stripped);
  if (kind.TAG !== "Ok") {
    return {
      TAG: "Error",
      _0: {
        TAG: "Ws",
        _0: {
          TAG: "DeserializationError",
          _0: kind._0.message
        }
      }
    };
  }
  let version;
  version = typeof stripped === "object" && stripped !== null && !Array.isArray(stripped) ? Stdlib_Option.getOr(optFloat(stripped, "version"), 0.0) : 0.0;
  return {
    TAG: "Ok",
    _0: {
      kind: kind._0,
      version: version
    }
  };
}

let MarketEvent = {};

let AuthUpdate = {};

let WsPriceHistory = {};

let WsDepositPrice = {};

let WsDepositAssetPrice = {};

let UserUpdate = {};

export {
  MarketEvent,
  AuthUpdate,
  WsPriceHistory,
  WsDepositPrice,
  WsDepositAssetPrice,
  UserUpdate,
  decodeMessage,
}
/* Order Not a pure module */
